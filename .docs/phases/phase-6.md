# Phase 6 — Скидки, отзывы и главная страница

## Цель

Менеджер включает скидку у Варианта — и витрина везде (мини-карточка,
карточка товара, корзина, оформление) показывает зачёркнутую старую
цену и новую, сумма корзины и снэпшот Позиции Заказа считаются по
новой; каталог фильтрует «Со скидкой». Покупатель оставляет отзыв о
Товаре на карточке, Менеджер модерирует отзывы (о Товаре и о магазине)
в Панели управления. Главная получает живые блоки — баннеры из БД,
«Хиты продаж», «Отзывы о магазине», «Новинки», «Товары со скидкой» —
`FR-DISC-001…002`, `FR-CAT-010`, `FR-CARD-006`, `FR-ADM-004`,
`FR-HOME-001…008`, `BR-002`.

Не входит: редактирование баннеров и текста «О компании» в Панели
управления (`FR-ADM-003` → Фаза 8); избранное — иконка на мини-карточке
и перенос из корзины (`FR-CAT-009`, `FR-CART-004` → Фаза 7); промокоды
(`Q-031`, вне MVP). `FR-HOME-002` (меню категорий) и `FR-HOME-003`
(поиск в шапке) уже сделаны в Фазе 1 — в этой фазе только регрессионно
проверяются, не строятся.

## Статус

🔄 В работе

## Что уже готово (на чём строим)

- `reviews`, `banners`, `products.is_featured`,
  `product_variants.discount_percent` заведены в БД с Фазы 0
  (`ADR-015`, `ADR-023`, `ADR-024`) — **схема БД не меняется ни в одном
  таске фазы**, добавляется только сид `banners` в `install.php`.
- `discount_percent` и `is_featured` уже сохраняются формой Товара
  (Таск 8 Фазы 4) — но ни один запрос витрины их не читает:
  `getCatalogProducts()` (`MIN(pv.price)`), `getCartItems()`,
  `addCartItem()` (`price_snapshot`), `createOrder()`,
  `addOrderItem()`, `recalculateOrderTotal()` берут сырую `pv.price`.
- Тема витрины уже содержит разметку `.old-price`/`.sale-price`
  (`style.css`) и секции «New Products / Best Sellers / Sale Product»
  в `00-input/design/index.html` — сверх темы рисуются только карточки
  отзывов о магазине (`C-009`).
- `attachCheapestVariant()` — общий батч без N+1 для любого списка
  Товаров (каталог, поиск, похожие) — переиспользуется блоками Главной.
- `tooManyAttempts()`/`hitRateLimit()` (`functions.php`) — готовый
  rate-limit для формы отзыва, тот же паттерн, что у чекаута.
- Механизм «фильтры без перезагрузки» в `app.js` слушает все поля
  `#catalog-filter-form` — новый чекбокс фильтра подхватится без
  правок JS.

## Решения фазы

- **Единый источник скидочной цены — одно SQL-выражение + одна
  PHP-функция с одинаковым округлением** (Таск 1, `ADR-041`).
  `ROUND(price * (1 - pct/100), 2)` в SQL через хелпер
  `discountedPriceSql('pv')`; `discountedPrice(string, ?string): string`
  на bcmath — юнит-тест проверяет `40 000 / 15% → 34 000.00` и
  граничные копейки. Альтернатива — считать только в PHP после выборки
  — отвергнута: сортировка и фильтр по цене в каталоге идут в SQL,
  два места расчёта разойдутся.
- **`discount_percent = 0` трактуется как «скидки нет»** (`> 0` в
  запросах, `hasDiscount()` в PHP) — форма Фазы 4 пропускает `0`,
  нормализовать данные задним числом не нужно.
- **Снятие/включение скидки = изменение цены для `FR-CHK-003`**
  (уведомление «было/стало» на оформлении) — получается автоматически,
  раз `price_snapshot` хранит скидочную цену. Отдельной логики нет.
- **Баннеры Главной читаются из `banners`** (сид из 3 текущих слайдов в
  `install.php`), UI редактирования — Фаза 8 (`FR-ADM-003`). Иначе
  слайдер переделывался бы дважды.
- **Отзыв о магазине, вносимый Менеджером вручную (`FR-HOME-007`
  правило 5), создаётся сразу `approved`** — модератор и автор одно
  лицо, лишний клик «одобрить» не нужен. Отзывы с витрины — всегда
  `pending` (`FR-ADM-004` правило 1).
- **Форма отзыва — без регистрации (Гость), с rate-limit
  `tooManyAttempts('review', 3, 600)`** по аналогии с чекаутом; email
  автора хранится, но публично не показывается. Авторизованному
  Покупателю имя/email подставляются.
- **Размер блоков Главной — 8 Товаров** во всех трёх (`FR-HOME-005`
  задаёт 8 для «Новинок»; для «Хитов» и «Со скидкой» ТЗ число не задаёт
  — берётся то же). Пустой блок скрывается целиком
  (`FR-HOME-004`/`007`/`008`).
- **Средний рейтинг Товара показывается только на карточке товара**
  (вкладка отзывов), на мини-карточке — нет: `FR-CARD-006` требует
  форму «как есть», расширять мини-карточку ТЗ не просит.
- **Деньги** — только строками/`DECIMAL`, сравнение через `bccomp()`,
  `formatPrice()` только во Views (`php.md`), как в Фазах 2–5.

## Таски

| #   | Название                                               | Статус      |
| --- | ------------------------------------------------------ | ----------- |
| 1   | Скидочная цена: единый источник истины (Model, без UI) | ✅ Завершён |
| 2   | Старая/новая цена на витрине                           | ✅ Завершён |
| 3   | Фильтр каталога «Со скидкой»                           | ⏳ Ожидает  |
| 4   | Отзывы о Товаре: форма и блок на карточке              | ⏳ Ожидает  |
| 5   | Модерация отзывов в Панели управления                  | ⏳ Ожидает  |
| 6   | Главная: баннеры, хиты, новинки, отзывы, скидки        | ⏳ Ожидает  |

---

## Таск 1 — Скидочная цена: единый источник истины

**Статус:** ✅ Завершён 20.09.2026 — проверено на реальной БД
(`mikhail700.beget.tech`). Подробности — `.docs/dev-log.md` 20.09.2026.
Отклонение от плана: дополнительно изменена `acceptCartPriceChanges()`
(`Cart.php`) — не была в исходном списке файлов, но без правки снэпшот
корзины после «Принять изменения» расходился бы со скидочной ценой
`getCartItems()`.

**Цель таска:**
Все точки, где витрина, корзина и Заказ читают цену Варианта, отдают
скидочную цену (`FR-DISC-001` правило 3: цена минус процент;
`FR-DISC-002` правило 2: скидочная цена копируется в Позицию Заказа).
Сортировка и фильтр по цене в каталоге — по скидочной. Внешне ничего
не меняется, кроме цифр — UI старой/новой цены строит Таск 2.

**Что нужно создать/изменить:**

- `src/Core/Price.php` — создать: `discountedPrice(string $price,
?string $percent): string` (bcmath, округление до копеек half-up),
  `hasDiscount(?string $percent): bool` (`NULL`/`0` → нет),
  `discountedPriceSql(string $alias): string` — SQL-выражение
  `ROUND(alias.price * (1 - IFNULL(alias.discount_percent, 0) / 100), 2)`
  — чистые функции без БД
- `tests/Unit/PriceTest.php` — создать; `tests/bootstrap.php` —
  подключить `Core/Price.php`
- `src/Models/Product.php` — изменить: `getCatalogProducts()`,
  `countCatalogProducts()`, `buildCatalogFilterConditions()` (фильтр
  `price_min`/`price_max`), `getFilterOptions()` (границы слайдера),
  `searchProducts()`, `suggestProducts()`, `getRelatedProducts()`,
  `attachCheapestVariant()` (самый дешёвый — по скидочной цене),
  `getProductVariants()`, `searchVariantsForAdmin()` — `price` =
  скидочная, плюс `old_price` (сырая) и `discount_percent`; `min_price`
  каталога — минимум скидочных, плюс `min_old_price` для мини-карточки
- `src/Models/Cart.php` — изменить: `getCartItems()` (`price` =
  скидочная, `old_price`, `discount_percent`), `addCartItem()` — снэпшот
  скидочной цены
- `src/Models/Order.php` — изменить: `createOrder()`, `addOrderItem()`,
  `recalculateOrderTotal()` — снэпшот и пересчёт по скидочной цене
- `.docs/planning-log.md`, `.docs/dev-log.md` — изменить: `ADR-041`
  (единое SQL/PHP-выражение, `0` = нет скидки, скидка как изменение
  цены для `FR-CHK-003`)

**Definition of Done:**

- [ ] `PriceTest`: `40000.00`/`15` → `34000.00`; `NULL` и `0` → цена без
      изменений; `99.99`; округление копеек (`1999.99`/`33`) совпадает
      с `SELECT ROUND(...)` в MySQL — ручная сверка одной выборкой
- [ ] Вариант 40 000 ₽ со скидкой 15%: каталог сортирует по цене и
      фильтрует по диапазону как 34 000; корзина — строка и `total` по
      34 000; после оформления `order_items.price = 34000.00`,
      `orders.total` по новой цене
- [ ] Скидка снята после оформления → `order_items.price` и
      `orders.total` не изменились (критерий приёмки `FR-DISC-002`)
- [ ] Включение скидки у Варианта, уже лежащего в корзине → на
      `/checkout` уведомление «было/стало» без правок кода чекаута
- [ ] `grep -rn "pv.price" src/` — сырая цена читается только в
      админ-форме Варианта (`getAllProductVariants()`) и внутри
      `discountedPriceSql()`
- [ ] `composer test` зелёный, включая `PriceTest`
- [ ] Проверить `.docs/dod-global.md`

---

## Таск 2 — Старая/новая цена на витрине

**Статус:** ✅ Завершён 20.09.2026 — проверено на реальной БД и живым
HTTP. Подробности — `.docs/dev-log.md` 20.09.2026. Scope сокращён
против исходного плана по факту сверки с разметкой темы: без бейджа
«-N%», без правок `checkout-summary.php`/`variant-selector.php`/
`app.css` — обсуждено и подтверждено пользователем перед реализацией,
см. файлы «Что нужно создать/изменить» ниже.

**Цель таска:**
`FR-DISC-002` — мини-карточка каталога/поиска/похожих, карточка товара
(в том числе при смене Варианта без перезагрузки) и строка корзины
показывают зачёркнутую старую цену рядом с новой — точно в разметке
темы (`.old-price`/`.sale-price`, `.product-prices` в корзине), сверх
шаблона ничего не дорисовывается (`FR-DISC-002` правило 1: без бейджа
«-N%» — в теме его нет; без правок сводки оформления — там нет цены за
штуку вообще, только сумма строки, уже верная с Таска 1).

**Что нужно создать/изменить** (список ниже — фактический, после
сверки исходного плана с разметкой темы):

- `src/Controllers/ProductController.php` — изменено:
  `old_price_formatted`, `discount_percent` в `$variantsData`
- `src/Views/product/show.php` — изменено: `#product-old-price` рядом
  с `#product-price`, `hidden` через `hasDiscount()` (SSR первого
  Варианта)
- `public/assets/js/app.js` — изменено: `renderVariant()` —
  показывает/прячет и обновляет `#product-old-price` при переключении
  Варианта
- `src/Views/components/product-card.php` — изменено: `<span
  class="old-price">` в `.price` (grid и list), рендерится только при
  `$product['min_old_price'] !== null` (уже посчитано в Model, Таск 1)
- `src/Views/components/cart-row.php` — изменено: `.product-prices` со
  старой+новой ценой вместо одиночного `<p class="price">`, когда
  `hasDiscount($item['discount_percent'])`

**Definition of Done:**

- [x] Вариант со скидкой: мини-карточка (grid/list, каталог/поиск/
      похожие), карточка товара, корзина — старая цена зачёркнута,
      новая рядом; без скидки — одна цена, в HTML нет пустых
      `.old-price`
- [x] Карточка: переключение Варианта со скидкой ↔ без скидки
      корректно показывает/прячет старую цену через JS; SSR первого
      Варианта верный без JS
- [x] Сумма корзины и чекаута — по новой цене (проверено на реальной
      скидке 15%: 35 000 → 29 750, и на уже существовавшей в БД
      скидке 20% на другом Товаре: 32 000 → 25 600)
- [x] Весь вывод через `e()`/`formatPrice()`; в HTML нет внешних
      адресов
- [x] `composer test` зелёный (271/271, регрессия)
- [x] Проверить `.docs/dod-global.md`

---

## Таск 3 — Фильтр каталога «Со скидкой»

**Статус:** ⏳ Ожидает

**Цель таска:**
`FR-CAT-010` — пятый фильтр сайдбара (`FR-CAT-002`): отбирает Товары,
у которых хотя бы один активный Вариант со скидкой (`FR-DISC-001`
правило 4); применяется без перезагрузки, как остальные (`FR-CAT-001`),
в URL — `on_sale=1`.

**Что нужно создать/изменить:**

- `src/Core/CatalogFilters.php` — изменить: `normalizeCatalogOnSale()`,
  ключ `on_sale` в `normalizeCatalogFilters()` и
  `buildCatalogQueryString()`
- `tests/Unit/CatalogFiltersTest.php` — изменить: `1`/`0`/мусор/
  отсутствие, сериализация в query string
- `src/Models/Product.php` — изменить: условие
  `EXISTS (... pv.is_active = 1 AND pv.discount_percent > 0)` в
  `buildCatalogFilterConditions()`
- `src/Views/components/catalog-sidebar.php` — изменить: чекбокс «Со
  скидкой» по образцу «В наличии»
- `src/Controllers/CatalogController.php`,
  `src/Controllers/SearchController.php` — изменить только если фильтр
  не проходит через общий `normalizeCatalogFilters()`

**Definition of Done:**

- [ ] Скидка у одного Варианта → Товар в выдаче фильтра; скидка снята у
      единственного Варианта со скидкой → Товар пропал (критерии
      приёмки `FR-DISC-001`)
- [ ] `on_sale=1` переживает F5 и «назад»/«вперёд» (общий механизм
      `app.js` — проверить, не дописывать); мусор в параметре
      игнорируется, не 500
- [ ] Сочетание с «В наличии», ценой, категорией, цветом — `AND`;
      пагинация и сортировка сохраняют параметр в ссылках
- [ ] Пустая выдача → существующее пустое состояние с «Сбросить
      фильтры», сброс снимает и этот чекбокс
- [ ] `composer test` зелёный, включая новые кейсы `CatalogFiltersTest`
- [ ] Проверить `.docs/dod-global.md`

---

## Таск 4 — Отзывы о Товаре: форма и блок на карточке

**Статус:** ⏳ Ожидает

**Цель таска:**
`FR-CARD-006` — на карточке товара появляется вкладка отзывов: список
одобренных (имя, рейтинг, дата, текст), средний рейтинг и число
отзывов, форма (имя, email, рейтинг 1–5, текст) из `SCR-03` «как
есть». Отправленный отзыв всегда `pending` и не виден никому до
модерации (`FR-ADM-004` правило 1); Покупатель видит flash «отзыв
появится после проверки».

**Что нужно создать/изменить:**

- `src/Core/Review.php` — создать: `REVIEW_STATUS_PENDING/_APPROVED/
_REJECTED`, `reviewStatusLabel()`, `validateReviewInput(array):
array` (имя 2–150, email через `validateEmail()`, рейтинг — целое
  1–5, текст непустой, лимит длины), `averageRating(array $reviews):
?string` — чистые функции
- `tests/Unit/ReviewTest.php` — создать; `tests/bootstrap.php` —
  подключить
- `src/Models/Review.php` — создать: `createReview(array $data): ?int`
  (`status='pending'`, только для активного Товара),
  `getApprovedProductReviews(int $productId): array`,
  `getProductRatingSummary(int $productId): array` (`avg`, `count`)
- `src/Controllers/ReviewController.php` — создать: `store(string
$slug)` — `requireCsrf()`, `tooManyAttempts('review', 3, 600)` /
  `hitRateLimit('review')`, валидация → ошибки и значения в сессию,
  POST → `redirect()` на карточку с якорем `#reviews`
- `src/Views/components/review-form.php`,
  `src/Views/components/review-list.php` — создать (`csrfField()`,
  подстановка имени/email авторизованного Покупателя)
- `src/Views/product/show.php`, `src/Controllers/ProductController.php`
  — изменить: вкладка «Отзывы» вместо комментария-заглушки, передача
  отзывов и сводки рейтинга
- `config/routes.php` — изменить: `POST /product/{slug}/reviews`

**Definition of Done:**

- [ ] Отправленный отзыв: строка `reviews` со `status='pending'`,
      `product_id` заполнен; на карточке **не виден** (критерий приёмки
      `FR-ADM-004`) — проверено на реальной БД
- [ ] Рейтинг вне 1–5 / пустой текст / невалидный email → ошибки у
      полей, введённые значения сохранены; 419 без CSRF; 4-я отправка за
      10 минут → flash-ограничение, запись не создана
- [ ] Вручную `approved` в БД → отзыв, средний рейтинг и счётчик видны;
      `rejected`/`pending` — нет
- [ ] Отзыв на несуществующий/неактивный Товар (подмена `slug`) → 404 /
      отказ, запись не создана
- [ ] Email автора нигде не выводится в HTML; весь вывод через `e()`
- [ ] `composer test` зелёный, включая `ReviewTest`
- [ ] Проверить `.docs/dod-global.md`

---

## Таск 5 — Модерация отзывов в Панели управления

**Статус:** ⏳ Ожидает

**Цель таска:**
`FR-ADM-004` — `/admin/reviews`: очередь модерации с фильтром по
статусу (по умолчанию `pending`), тип «о Товаре» (ссылка на Товар) /
«о магазине», действия «Одобрить» / «Отклонить», бейдж с числом
`pending` в сайдбаре. Плюс форма ручного добавления отзыва о магазине
(`FR-HOME-007` правило 5: Владелец переносит отзывы из
Instagram/WhatsApp) — `product_id = NULL`, сразу `approved`. Автор
отклонённого отзыва не уведомляется (`FR-ADM-004` правило 3). Паттерн
— `admin-assembly.md` («Модерация отзывов», `blog-details.html`).

**Что нужно создать/изменить:**

- `src/Models/Review.php` — изменить: `getAdminReviews(array $filters,
int $page, int $perPage): array` (`LEFT JOIN products`),
  `countAdminReviews()`, `setReviewStatus(int $id, string $status):
bool`, `countPendingReviews(): int`, `createStoreReview(array
$data): ?int` (`product_id = NULL`, `status='approved'`)
- `src/Controllers/AdminReviewController.php` — создать: `index()`
  (whitelist статуса через `REVIEW_STATUS_*`, `buildPagination()`),
  `approve()`, `reject()`, `storeShopReview()` — все
  `requireRole(['manager','admin'])`, POST — `requireCsrf()` →
  `redirect()`
- `src/Views/admin/reviews/index.php` — создать: фильтр статуса,
  список (автор, рейтинг, тип со ссылкой, дата, текст, действия),
  пустое состояние, серверная пагинация, форма добавления отзыва о
  магазине
- `src/Views/layout/admin-header.php` — изменить: пункт «Отзывы» с
  бейджем `pending`, активный пункт
- `config/routes.php` — изменить: `GET /admin/reviews`,
  `POST /admin/reviews`, `POST /admin/reviews/{id}/approve`,
  `POST /admin/reviews/{id}/reject`
- `tests/Unit/ReviewTest.php` — изменить: `validateReviewInput()` для
  формы отзыва о магазине (тот же валидатор, без `product_id`)

**Definition of Done:**

- [ ] Одобрение → отзыв появляется на карточке Товара (критерий приёмки
      `FR-ADM-004`); отклонение → не появляется, никаких писем/СМС
      автору — проверено на реальной БД
- [ ] Повторное «Одобрить» на `approved` — идемпотентно; несуществующий
      id → flash «не найден», данные не изменены
- [ ] Отзыв о магазине из формы: `product_id IS NULL`,
      `status='approved'`; валидация — та же, что у витринной формы
- [ ] Фильтр `pending`/`approved`/`rejected`/все; неизвестное значение
      игнорируется, не 500; бейдж в сайдбаре совпадает с
      `SELECT COUNT(*) FROM reviews WHERE status='pending'`
- [ ] `customer` по `/admin/reviews` → редирект; Гость → `/login`; 419
      без CSRF
- [ ] `composer test` зелёный
- [ ] Проверить `.docs/dod-global.md`

---

## Таск 6 — Главная: баннеры, хиты, новинки, отзывы, скидки

**Статус:** ⏳ Ожидает

**Цель таска:**
`FR-HOME-001, 004…008` — слайдер читает `banners`; блоки Товаров «Хиты
продаж» (`is_featured`), «Новинки» (8 последних по `created_at`),
«Товары со скидкой» (хотя бы один Вариант со скидкой); блок «Отзывы о
магазине» сразу после «Хитов» (`C-009`, `FR-HOME-007` правило 1) из
одобренных отзывов с `product_id IS NULL`; блок «О компании» —
статичный текст (редактирование — Фаза 8). Каждый блок Товаров/отзывов
скрыт целиком, если пуст.

**Что нужно создать/изменить:**

- `src/Models/Product.php` — изменить: `getFeaturedProducts(int
$limit)`, `getNewestProducts(int $limit)`,
  `getDiscountedProducts(int $limit)` — только активные Товары с
  активным Вариантом, через общий `attachCheapestVariant()`
- `src/Models/Banner.php` — создать: `getActiveBanners(): array`
  (`is_active = 1 ORDER BY sort_order`); `database/install.php` —
  изменить: сид 3 текущих слайдов (`INSERT IGNORE`, как
  `sales_channels`)
- `src/Models/Review.php` — изменить: `getApprovedStoreReviews(int
$limit): array`
- `src/Controllers/HomeController.php` — изменить: сбор данных для всех
  блоков, константа `HOME_BLOCK_LIMIT = 8`
- `src/Views/home.php` — переписать: слайдер из `$banners`, блоки в
  порядке ТЗ; `src/Views/components/home-product-section.php`
  (заголовок + сетка из `product-card.php`, ничего не рисует при пустом
  списке), `src/Views/components/store-review-card.php` — создать
- `public/assets/css/app.css` — изменить: карточки отзывов о магазине
  (`C-009`, BEM, mobile-first)

**Definition of Done:**

- [ ] Отметка «хит» у Товара / скидка у Варианта / одобренный отзыв о
      магазине → появляется в своём блоке; снятие последнего → блок
      исчезает целиком, не показывает пустую сетку (критерии приёмки
      `FR-HOME-004/007/008`) — проверено на реальной БД
- [ ] «Новинки» — ровно 8 по `created_at DESC`, неактивный Товар и
      Товар без активных Вариантов не попадают
- [ ] Блок отзывов стоит сразу после «Хитов»; отзыв о Товаре
      (`product_id` заполнен) в нём не показывается; `pending` — нет
- [ ] Слайдер показывает строки `banners` в порядке `sort_order`;
      `is_active = 0` → слайд скрыт; `install.php` дважды подряд без
      дублей
- [ ] Мини-карточки блоков показывают старую/новую цену (Таск 2) и
      ведут на карточку; на Товар — не более одного батч-запроса
      (`attachCheapestVariant`), без N+1
- [ ] Меню категорий и поиск в шапке (`FR-HOME-002/003`) работают —
      регрессия; в HTML нет внешних адресов; читаемо на 320px
- [ ] `composer test` зелёный (регрессия)
- [ ] Проверить `.docs/dod-global.md`

---

## При закрытии фазы

- `_status.md` — статус Фазы 6 → ✅
- `tz-coverage.md` — `FR-DISC-001…002`, `FR-CAT-010`, `FR-CARD-006`,
  `FR-ADM-004`, `FR-HOME-001…008`, `BR-002` → Реализовано с указанием
  тасков; у `FR-HOME-001` — пометка «слайдер из `banners`,
  редактирование в Панели — Фаза 8 (`FR-ADM-003`)»; у `FR-HOME-002/003`
  — ссылка на Фазу 1
- `.docs/dev-log.md` — решения фазы: единое SQL/PHP-выражение
  скидочной цены, `0` = нет скидки, ручной отзыв о магазине сразу
  `approved`, баннеры из БД до появления UI редактирования
