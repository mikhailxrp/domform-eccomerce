# Журнал планирования

Решения, принятые при переносе ТЗ в документацию для разработки.
Не путать с `.docs/dev-log.md` — тот ведётся уже во время написания
кода.

| Дата | ADR | Решение | Источник в ТЗ | Почему | Отвергнуто |
|---|---|---|---|---|---|
| 2026-09-11 | ADR-001 | В `prd.md` роли перенесены кратко (роль/кто/что делает), матрица прав раздела 5.2 не копируется целиком | Раздел 5 ТЗ | Матрица нужна разработчику по ходу конкретных тасков, не на старте — держим PRD читаемым | Скопировать всю матрицу в prd.md — раздувает файл, дублирует источник истины |
| 2026-09-11 | ADR-002 | ЮMoney зафиксирован в `prd.md` §«Стек» как дополнение к базовому CLAUDE.md | Раздел 6.3 ТЗ (диаграмма статусов оплаты) | Единственный названный в прочитанных на этом шаге разделах платёжный провайдер — сверх стека стартера | — |
| 2026-09-11 | ADR-003 | `users.role` расширен до `ENUM('customer','manager','admin')`, было `ENUM('customer','admin')` | Раздел 5.1 ТЗ (4 роли: Гость/Покупатель/Менеджер/Администратор) | Явное указание пользователя: 2 значений недостаточно для 4 ролей ТЗ, «Гость» в БД не хранится → минимум 3. Закрывает `Q-DEV-001` | Оставить `admin` для обеих ролей — теряет различие прав раздела 5.2 (управление пользователями — только Администратор) |
| 2026-09-11 | ADR-004 | Сущность «Вариант товара» вынесена в новую таблицу `product_variants`; `products.sku`/`price`/`stock_quantity` удалены (не оставлены как второй источник истины) | Раздел 6.1 («Вариант... имеет собственные: артикул, материал, цену, срок изготовления»), 6.2 (цена/артикул различаются между Вариантами) | Товар и Вариант — разные сущности в модели ТЗ с разными наборами полей; оставить цену на `products` создало бы два конфликтующих источника цены | Хранить варианты как строки `product_attributes` (паттерн шаблона) — не покрывает собственные артикул/цену/срок на вариант, только атрибут-значение |
| 2026-09-11 | ADR-005 | Связь Товар↔Категория переведена в M:N через `product_categories` (с `is_primary`), `products.category_id` удалён | Раздел 6.1 `[Б]`: «Товар может входить в несколько Категорий одновременно» (пример: модульный диван — и в «Диванах», и в «Для кухни-гостиной») | Прямое требование ТЗ, буквально противоречит исходной 1:N | Оставить `category_id` как «основную» категорию и добавить отдельную M:N-таблицу параллельно — два места хранения одного факта принадлежности |
| 2026-09-11 | ADR-006 | `product_images` переименована в `variant_images`, ссылается на `product_variant_id`, добавлены `color`/`is_swatch` | Раздел 6.1 («Вариант... имеет 0..N Фотографий — для части цветов реальные, для остальных образец ткани»), 6.2 (цвет — атрибут, различаются только фото) | Фото принадлежат Варианту, не Товару (у каждого Варианта своя раскладка по цветам) | Оставить фото на `products` и добавлять цвет отдельной таблицей — не отражает, что фото различаются именно по Варианту |
| 2026-09-11 | ADR-007 | Новая таблица `reserves` (`order_item_id`, `product_variant_id`, `status`, `agreed_until`) | Раздел 6.1 (сущность «Резерв»), `BR-003` (момент создания/списания, конкуренция за образец, устный срок без таймера) | Резерв — сущность из доменной модели ТЗ с явными связями и жизненным циклом (`active/released/fulfilled`) | Хранить резерв как булево поле на `product_variants` — не покрывает привязку к конкретному Заказу/Позиции и историю снятий |
| 2026-09-11 | ADR-008 | `orders` расширен: `fulfillment_method`, `delivery_address`, `shipping_cost`, `guest_name/phone/email`, `payment_method`, `payment_status`, `prepaid_amount`, `delivered_at`; `status` расширен с 4 до 7 значений | Раздел 6.1 (способ получения, гость), 6.3 (7-статусная диаграмма Заказа + отдельная диаграмма Оплаты), `BR-001` (способы оплаты), `BR-006` (ручная стоимость доставки) | Базовый `ENUM('created','paid','shipped','cancelled')` не покрывает жизненный цикл раздела 6.3; оплата и статус заказа — независимые оси в ТЗ, не одно поле | Оставить 4 статуса и трактовать «paid» как статус заказа — противоречит 6.3, где оплата не заменяет ни один статус жизненного цикла |
| 2026-09-11 | ADR-009 | `order_items`: `product_id`→`product_variant_id`, снэпшот развёрнут в поля `variant_sku/material/mechanism/color` вместо одного `product_name` | Раздел 6.1 («хранит КОПИЮ... артикул и характеристики Варианта») | Позиция заказа теперь ссылается на Вариант (там цена/артикул), характеристики перечислены в ТЗ поимённо — удобнее структурированно, чем одной строкой | Хранить характеристики одной текстовой колонкой — сложнее выводить в Панели управления (`FR-MGR-*`, раздел 8, не читался, но структура уже предсказуема из 6.1) |
| 2026-09-11 | ADR-010 | `cart_items.product_id` → `product_variant_id`, добавлено поле `color` | Раздел 6.2 (цена/остаток различаются по Варианту; цвет не создаёт отдельный артикул) | Корзина должна хранить ссылку на то, что определяет цену — Вариант, а не Товар; цвет нужен для отображения и снэпшота при оформлении | — |
| 2026-09-11 | ADR-011 | Скидка — поле `product_variants.discount_percent` (DECIMAL(5,2)), не отдельная таблица | `BR-002` («Тип скидки — процент от цены... включается/снимается вручную, без даты начала/окончания») | Ровно то, что описывает правило — не нужна ни отдельная сущность, ни даты действия | Таблица `discounts` с датами start/end — ТЗ прямо исключает даты действия для скидки на Вариант |
| 2026-09-11 | ADR-012 | Новая таблица `returns` (`order_id`, `note`, `created_at`) без статусной модели | Раздел 6.1 (сущность «Возврат»), 6.3 («не имеет собственных промежуточных статусов на сайте, процесс офлайн») | Возврат явно перечислен как сущность с связью на Заказ — минимальная таблица для факта обращения, без статусов, которых ТЗ прямо не требует | Не заводить таблицу вообще — 6.1 всё же описывает Возврат как сущность со связью, а не как «не хранится» (в отличие от явной формулировки для Гарантии) |
| 2026-09-11 | ADR-013 | Гарантия не заведена отдельной таблицей; добавлено `orders.delivered_at` для расчёта 18 месяцев на лету | Раздел 6.1 (`[Р]`: «Гарантия... не хранится на сайте как отдельная сущность с жизненным циклом») | Прямая формулировка ТЗ — вычислимый факт, не сущность | Таблица `warranties` — ТЗ явно возражает против отдельной сущности |
| 2026-09-11 | ADR-014 | Ни одной таблице не добавлены `external_id`/`synced_at` | Раздел 10.1 ТЗ («Нет внешней системы» — по каждому типу данных) | 10.1 явно и по каждой строке подтверждает отсутствие интеграций — место под синхронизацию было бы домыслом | Добавить `external_id` про запас — прямо запрещено правилом 1 системного промпта («не изобретай функциональность, которой нет в tz.md») |
| 2026-09-12 | ADR-015 | Новая таблица `reviews` (`product_id` NULL, `name`, `email`, `rating`, `text`, `status`) — одна сущность для отзыва о Товаре и отзыва о магазине | `FR-CARD-006`, `FR-HOME-007`, `FR-ADM-004` (раздел 8 прочитан на этом шаге) | Оба вида отзыва проходят одну и ту же модерацию (`FR-ADM-004`) и различаются только тем, заполнен ли `product_id` — заводить две таблицы дублировало бы модерацию | Две отдельные таблицы `product_reviews`/`store_reviews` — общий `status`/модерация стали бы логикой на двух таблицах вместо одной |
| 2026-09-12 | ADR-016 | Новая таблица `favorites` (`user_id`, `product_id`, `UNIQUE(user_id, product_id)`) | `FR-ACC-003`, `FR-CAT-009`, `FR-CART-004` | «Список Товаров» (`FR-ACC-003`) — раздел личного кабинета, доступен только авторизованному Покупателю (Гостю кнопка не показывается, `FR-CART-004`) — в отличие от `cart_items`, `session_id` не нужен | Хранить как JSON-список в `users` — не даёт индекса/уникальности и усложняет выборку каталога «уже в избранном» |
| 2026-09-12 | ADR-017 | Функциональность «Избранное» (иконка на мини-карточке `FR-CAT-009`, перенос из корзины `FR-CART-004`) и форма/блок отзыва на карточке товара (`FR-CARD-006`) **не строятся** в Фазах 1–2 вместе с остальным диапазоном своих `FR-*`, а переезжают в Фазы 7 и 6 соответственно, где уже есть `favorites`/`reviews` и их обвязка (модерация, личный кабинет) | Находка `/sdd-review` перед `phase-init`: обе функции иначе оказывались на более ранней фазе, чем таблицы, которые они требуют — обратный порядок зависимости | Портфолио-характер проекта (решение Заказчика, 2026-09-12): часть UI в ранних фазах осознанно не полностью рабочая — это принято, а не скрыто | «Заглушка» (кнопка есть, но не пишет в БД) в Фазах 1–2 — отклонено: создаёт технический долг «доделать позже», который легко потерять; явный перенос диапазона `FR-*` в `_status.md`/`tz-coverage.md` нельзя не заметить на `phase-init` |
| 2026-09-12 | ADR-018 | Оплата картой на сайте (`FR-PAY-001…005`, модуль `PAY`) реализуется как страница-заглушка вместо реальной интеграции с ЮMoney: редирект на собственный экран «демо, для оплаты свяжитесь с Менеджером», без вебхука/подписи/`payment_logs`; фискализация через Атол (`FR-PAY-005`) не реализуется вовсе | Решение Заказчика (портфолио-проект, не реальный бизнес-запуск, 2026-09-12) — не раздел ТЗ | Цель проекта — показать работающий магазин и встраивание автоматизаций/ИИ, не провести реальный платёж; снимает блокировку `Q-007`/`RISK-002` целиком (договор с ЮMoney для портфолио не нужен), а не откладывает её до фазы | Интеграция с ЮMoney в тестовом/sandbox-режиме — отклонено: требует реального подключения к песочнице провайдера, не нужного для цели показать сайт и ИИ-автоматизации |
| 2026-09-12 | ADR-019 | Консультант в чате (`FR-AI-003`) отвечает по правилам, зашитым вручную в системный промпт (контент страниц `CNT`/FAQ переносится туда при написании промпта), а не через базу знаний/retrieval по живым страницам. `FR-AI-001/002/004` не меняются — реальный вызов модели, реальные данные каталога | Решение Заказчика (портфолио-проект, 2026-09-12) | Поведенческое требование `FR-AI-003` («не отвечать вне круга тем») выполняется промптом так же, как retrieval-пайплайном; RAG-инфраструктура по `CNT`-страницам — не цель портфолио-демонстрации ИИ-интеграции | Строить RAG/индексацию страниц `CNT` — отклонено как избыточная инфраструктура для учебного проекта; риск расхождения промпта с `CNT` при будущем редактировании — принят осознанно, не скрыт |
| 2026-09-15 | ADR-020 | Отобранные страницы шаблона админки (Valex) куратируются в `00-input/admin/` — плоская раскладка `*.html` + `assets/` целиком, пути `../assets/…` переписаны на `assets/…` | `sdd-admin`, раздел 8.15/8.16 ТЗ | Точное зеркало уже принятого паттерна `00-input/design/` для витрины (тот же приём: цельная тема скопирована и урезана до нужных страниц, плоско, с рабочими относительными путями для предпросмотра в браузере) — не изобретаю новую структуру для админки | Копировать сразу в `src/Views/admin/`/`public/assets/admin/` как рабочий код — на этапе планирования код не пишется (правило 5 системного промпта `sdd-planning-agent.md`), конвертация в PHP/Views — задача `phase-init`/`task-init` |
| 2026-09-15 | ADR-021 | Страница «Клиенты» (`FR-ADM-002`, `FR-MGR-004`) размечена на готовую `contacts.html`, а не на `admin-missing-pages.json` — там её вообще нет | `admin-pages-manifest-classified.json` (id `contacts`, «универсально: адресная книга для любого CRM/админки») | Готовый паттерн покрывает список клиентов с контактами впрямую, без сборки из нескольких блоков | Заводить её как «недостающую» и собирать из `data-tables.html` — не нужно, когда есть более точный по смыслу готовый экран |
| 2026-09-15 | ADR-022 | Новая таблица `content_pages` (`slug`, `title`, `body`, `image_path`) — по одной строке на каждую статическую страницу и на каждый юридический документ | `FR-ADM-003` правило 1, `FR-CNT-001…006` | Находка `/sdd-review` перед `phase-init`: `FR-ADM-003` требует редактирования текста страниц Менеджером/Администратором без разработчика — в схеме не было ни одной таблицы, куда этот текст можно было бы записать (`Q-DEV-009`) | Структурированные колонки под каждую страницу отдельно (`contacts.address`, `contacts.phone`, ...) — отклонено: `FR-ADM-003` правило 3 прямо ограничивает редактирование содержимым (текст, фото), не структурой страницы — значит достаточно одного текстового поля на страницу, структура остаётся во View |
| 2026-09-15 | ADR-023 | Новая таблица `banners` (`image_path`, `title`, `link`, `sort_order`, `is_active`) | `FR-ADM-003` правило 2, `FR-HOME-001` | Тот же пробел, что `ADR-022`, для слайдера Главной — таблицы не было, баннеры было бы негде хранить (`Q-DEV-009`) | Хранить баннеры как строки в `content_pages` (например, `slug='home-banner-1'`) — отклонено: баннеров несколько, с собственным порядком показа (`sort_order`) и статусом активности — это список записей одного вида, а не текст одной страницы |
| 2026-09-15 | ADR-024 | `products.is_featured TINYINT(1) NOT NULL DEFAULT 0` — ручная отметка «хит продаж» | `FR-HOME-004` | Находка `/sdd-review`: ТЗ прямо требует ручную отметку Менеджером/Администратором (раздел 10.1 — нет справочника продаж на старте), по аналогии с уже принятым `product_variants.is_showroom_sample`, но колонки не было (`Q-DEV-010`) | Вычислять «хит продаж» по количеству проданных штук из `order_items` — отклонено: ТЗ явно описывает это как ручную отметку, не расчёт, автоматический подсчёт был бы домыслом функциональности сверх ТЗ |
| 2026-09-15 | ADR-025 | `users.is_blocked TINYINT(1) NOT NULL DEFAULT 0` — блокировка учётной записи | `FR-ADM-007` правило 3 | Находка `/sdd-review` (изначально замечено в `admin-assembly.md` при `sdd-admin`, но не поднято до `Q-DEV` в `tz-coverage.md`) — Администратор должен уметь заблокировать Менеджера, поля для этого не было (`Q-DEV-011`) | Удалять учётную запись вместо блокировки — отклонено: `orders.user_id` использует `ON DELETE SET NULL` именно чтобы пережить удаление аккаунта, но здесь нужна обратимая блокировка (разблокировать снова), а не потеря доступа Менеджера к своей истории действий |
| 2026-09-16 | ADR-026 | jQuery 3.5.1 разрешён во фронтенде витрины (self-hosted копия из темы, не CDN); запрет «только Vanilla JS» снят в `CLAUDE.md`/`general.md` | `phase-init` Фазы 0: ревизия `00-input/design/assets` | `main.js` (600 строк: sticky-header, off-canvas меню, слайдеры, рейтинг, +/− количества) и плагины темы (`nice-select`, `select2`, `zoom`, `ion.rangeSlider`, `ajax-contact`) написаны на jQuery — переписывать их на vanilla ради формального правила дороже, чем принять зависимость; Bootstrap-компоненты при этом остаются на нативном `data-bs-*` API | Переписать `main.js` и заменить плагины на vanilla-аналоги (+1–2 дня в Фазе 0 и по дню в Фазах 1/6) — отклонено Заказчиком 2026-09-16 как лишняя работа для портфолио-проекта |
| 2026-09-16 | ADR-027 | Новая таблица `password_resets` (`user_id`, `token_hash` sha256 UNIQUE, `expires_at`, `used_at`) | `FR-AUTH-003` (восстановление пароля) | Требование техническое (не буквальный пункт брифа, см. `prd.md` §«Роадмап» Фазы 0), но необходимо: без самостоятельного восстановления единственный Администратор становится единственным способом вернуть доступ каждому забывшему пароль Покупателю — не масштабируется; хэш, не сам токен — та же логика, что пароли пользователя | Хранить сам токен в открытом виде — отклонено: утечка строки БД тогда сразу даёт доступ к сбросу пароля любого пользователя, чей ряд не истёк |
| 2026-09-16 | ADR-028 | Новая таблица `remember_tokens` (`user_id`, `selector` UNIQUE, `token_hash` sha256, `expires_at`) — пара selector/validator, не продление cookie сессии | `FR-AUTH-004` («Remember me») | Продление `session.gc_maxlifetime` вместо отдельной таблицы отклонено: на shared-хостинге это общая настройка PHP, разделяемая с другими сайтами хостинга, недоступная для точечного продления одному приложению; отдельная таблица с собственным TTL — то же решение, что `password_resets`, без завязки на серверную конфигурацию | Единый токен без разбивки на selector/secret — отклонено: поиск активной строки по нему был бы либо полным сканированием таблицы со сравнением хэшей, либо прямым поиском по хэшу — тот же класс тайминг-риска, которого избегает пара selector (для быстрого `WHERE`) + secret (для `hash_equals()`) |
| 2026-09-17 | ADR-029 | `categories.description TEXT NULL` добавлена в схему | `FR-CAT-008` (описание категории под заголовком листинга) | Планирование Таска 1 Фазы 1: колонки под текст описания категории в схеме не было (пробел того же рода, что `Q-DEV-009`/`Q-DEV-010`) | Хранить описание категории в `content_pages` по аналогии со статическими страницами — отклонено: `content_pages` привязана к `slug` статической страницы (`ADR-022`), не к конкретной категории каталога, добавляла бы косвенную связь вместо прямой колонки |
| 2026-09-17 | ADR-030 | Новая таблица `product_specs` (`product_id`, `name`, `value`, `sort_order`) — таблица характеристик/размеров Товара | `FR-CARD-005` (таблица характеристик на карточке товара) | Планирование Таска 1 Фазы 1: характеристики нужны на уровне Товара (размеры/форма общие для всех Вариантов), а не Варианта, где уже есть `material`/`mechanism_type`; произвольный набор пар «название — значение» — раздел 8 ТЗ (точный список характеристик по категориям) не читался на этом шаге | Фиксированные колонки (`width`, `depth`, `height` на `products`) — отклонено: список характеристик различается по категориям (диван/шкаф/кровать), фиксированная схема не покрыла бы все случаи без NULL-колонок под каждую возможную характеристику |
| 2026-09-17 | ADR-031 | `INDEX(color)` добавлен на `variant_images` | `FR-CAT-003` (фильтр каталога по цвету), Таск 3 Фазы 1 | Фильтр цвета — новый запрос с `WHERE color IN (...)`, индекса на этой колонке не было (`dod-global.md`: «новый запрос с фильтром — под него есть индекс») | Оставить без индекса, полагаясь на малый размер таблицы на сидах — отклонено: правило `dod-global.md` требует индекс под новый фильтруемый запрос независимо от текущего объёма данных, не только когда он уже стал бы медленным |
| 2026-09-17 | ADR-032 | Поиск (Таск 5 Фазы 1): `MATCH...AGAINST (... IN BOOLEAN MODE)` по `products(name, description)` для запроса от 3 символов, префиксный `LIKE 'q%'` по `products.name`/`product_variants.material`/`variant_images.color` для 2 символов и материала/цвета в любом случае; добавлен `INDEX(material)` на `product_variants` | `FR-SRCH-001/002` | `innodb_ft_min_token_size = 3` — общий параметр сервера на shared-хостинге (`mikhail700.beget.tech`), недоступен для точечного изменения под конкретную БД, а подсказки нужны с 2 символов (`phase-1.md`, «Решения фазы»); материал — новый `LIKE`-фильтр, `dod-global.md` требует под него индекс, как и `color` в `ADR-031` | Отдельный `INDEX(name)` на `products` под 2-символьный `LIKE` — отклонено: тот же путь уже сознательно принят в `phase-1.md` («≤ 200 строк» на сидах, полный скан пренебрежимо дёшев), заводить индекс под путь, которым для тех же строк уже пользуется `FULLTEXT`, было бы вторым индексом ради редкого 2-символьного случая |
| 2026-09-18 | ADR-033 | `cart_items.price_snapshot DECIMAL(10,2) NOT NULL` добавлена в схему — используется **только** для сравнения «изменилась ли цена», сумма всегда пересчитывается из `product_variants` | `FR-CHK-003` правило 1 (уведомление «было / стало» на экране оформления), Таск 1 Фазы 2 | Чтобы показать «было X → стало Y» на `/checkout`, нужна цена на момент добавления в корзину — `cart_items` её не хранила | Снэпшот в сессии при открытии `/checkout` вместо колонки в БД — отклонено (`phase-2.md`, «Решения фазы»): не ловит изменение цены между добавлением в корзину и открытием чекаута, критерий приёмки `FR-CHK-003` не выполняется |
| 2026-09-18 | ADR-034 | `cart_items.session_id` с этого таска хранит значение cookie `cart_token` (32 байта `random_bytes` → 64 hex, 30 дней, httponly/samesite=Lax/secure в проде), а не PHP session id — схема колонки не меняется, меняется только источник значения | `FR-CART-002` правило 3 (корзина гостя переживает закрытие браузера) | PHP-сессия умирает при закрытии браузера и по GC на shared-хостинге (`ADR-028` — та же причина для remember-me) — корзина гостя на голой PHP-сессии не пережила бы повторный визит | Отдельная таблица `cart_tokens` для маппинга cookie → сессия — отклонено: `cart_items.session_id` уже VARCHAR(64) под произвольный идентификатор владельца, вторая таблица добавляла бы уровень косвенности без нового факта для хранения |
| 2026-09-18 | ADR-035 | `orders.comment TEXT NOT NULL` добавлена в схему | `FR-CHK-001` п.3-4 (обязательное поле «Комментарий» на форме оформления), Таск 4 Фазы 2 | ТЗ требует обязательный комментарий (ткань/размер/пожелания для звонка Менеджера) уже с Таска 3 Фазы 2 (форма его собирает), но колонка для хранения была пропущена при первичном переносе схемы `sdd-schema` — без неё `createOrder()` в Таске 4 не может его сохранить | — |
| 2026-09-18 | ADR-036 | Панель управления получает второй layout (`src/Views/layout/admin-header.php`/`admin-footer.php`) на ассетах шаблона Valex (`public/assets/admin/`), отдельный от витринного `header.php`/`footer.php`; свой JS панели — второй файл `public/assets/js/admin.js` (IIFE), не расширение витринного `app.js` | Таск 1 Фазы 4 (`admin-assembly.md`, `ADR-020`) | У Valex собственная сборка Bootstrap/сайдбар/иконки, несовместимая с темой витрины; `app.js` там же завязан на витринный `main.js` (`ADR-026`), которого в админке нет — правило «свой код в `app.js`» (`CLAUDE.md`) трактуется как «один файл на layout». Копия `styles.min.css` очищена от `@import` Google Fonts (единственный CDN-хост в ассетах Valex) — падает на системный `sans-serif` из того же `font-family` стека, без реальной альтернативы для shared-хостинга без CDN (`general.md`) | Использовать другую сборку `app.js`/`app.css` под оба layout — отклонено: два несовместимых набора Bootstrap-компонентов (`data-bs-*` от разных версий/тем) на одном файле усложнили бы его больше, чем плоское дублирование по layout |
| 2026-09-18 | ADR-037 | `INDEX(phone)` на `users`, `INDEX(guest_phone)` на `orders` — идемпотентно через `information_schema.STATISTICS`, тем же приёмом, что `ADR-031`/`ADR-032` | `FR-MGR-001` правило 2 (поиск Заказа по телефону), Таск 2 Фазы 4 | `getAdminOrders()` — новый запрос с поиском по `users.phone`/`orders.guest_phone` (точное совпадение по нормализованному `normalizePhone()`), `dod-global.md` требует индекс под новый фильтруемый/искомый запрос независимо от текущего объёма данных | Оставить без индекса, полагаясь на малый объём Заказов на портфолио-проекте — отклонено тем же аргументом, что уже был отклонён в `ADR-031`: правило `dod-global.md` не делает исключения по объёму данных |
| 2026-09-18 | ADR-038 | `orders.cancel_note TEXT NULL`, `orders.prepayment_refunded TINYINT(1) NOT NULL DEFAULT 0` добавлены в схему | `FR-ORD-002` правила 3 и 7 (форма отмены, Таск 3 Фазы 4) | Форма отмены собирает комментарий (обязателен для нестандартного размера) и отметку возврата предоплаты (обязательна, если оплата уже была) — колонок для их хранения не было | Отдельная таблица `order_cancellations` — отклонено: отмена одна на Заказ и не имеет собственной статусной модели (в отличие от `returns`, `ADR-012`, у которой есть независимый жизненный цикл), два поля на `orders` полностью описывают факт |
| 2026-09-19 | ADR-039 | Два новых демо-экрана Панели управления вне ТЗ: `/admin/sales-channels` («Каналы продаж» — WhatsApp/Авито/Telegram/MAX/сайт переключателями + статичный макет единого чата) и `/admin/integrations` («Интеграции» — CRM/1С/МойСклад/телефония/рассылки переключателями); новые таблицы `sales_channels`/`integrations`, обе только хранят состояние переключателя, реальных подключений нет | Прямой запрос заказчика (не пункт ТЗ) — показать возможность объединять заявки из разных каналов продаж и подключать внешние CRM/учёт | Тот же приём, что страница-заглушка оплаты (`ADR-018`, `/payment/stub`): демонстрирует идею функции портфолио-проекта без реальной интеграции, которую не с кем и незачем подключать на этом этапе | Не сохранять состояние переключателя в БД (только JS) — отклонено пользователем при уточнении задачи: демо должно переживать перезагрузку страницы |
| 2026-09-20 | ADR-040 | Проигрыш конкуренции за Выставочный образец откатывает фиксацию предоплаты целиком: `markOrderPrepaid()` создаёт `reserves.active` в той же транзакции, что `UPDATE payment_status`, и при SQLSTATE 23000 от `UNIQUE(active_variant_id)` делает `rollBack()` и возвращает `PREPAID_RESULT_SAMPLE_TAKEN` (строковый результат вместо `bool` — контроллеру нужно различать «уже оплачено» и «образец занят») | `BR-003`, `FR-STOCK-002…003`, UC-02 (2а), Таск 1 Фазы 5 | Оплаченный Заказ на образец, уже закреплённый за другим Покупателем, в системе оставаться не должен — Менеджер увидит его как обычный подтверждённый и не узнает о конфликте; откат делает конфликт видимым в момент действия (flash с номером Заказа-держателя) | Записать оплату, но резерв не создавать — отклонено (скрытый конфликт); доменное исключение из Model — отклонено: Model-функции проекта возвращают значения (`transitionOrderStatus()` → `false`, `createOrder()` → `null`), не бросают доменные ошибки |
| 2026-09-20 | ADR-041 | Скидочная цена Варианта — одна PHP-функция `discountedPrice()` (`bcmath`, округление half-up приёмом `bcadd(..., '0.005', 2)`) и одно эквивалентное SQL-выражение `discountedPriceSql()` (`ROUND(alias.price * (1 - IFNULL(alias.discount_percent, 0) / 100), 2)`), оба в новом `src/Core/Price.php`; заведены во все Model-функции, читающие цену Варианта для витрины/корзины/Заказа (`getCatalogProducts()`, `searchProducts()`, `attachCheapestVariant()`, `getProductVariants()`, `getCartItems()`, `addCartItem()`, `createOrder()`, `addOrderItem()` и др., Таск 1 Фазы 6); `discount_percent = NULL` или `0` — скидки нет (`hasDiscount()`) | `FR-DISC-001` правило 3 (цена минус процент), `FR-DISC-002` правило 2 (скидочная цена копируется в снэпшот Позиции Заказа), `BR-002` | Сортировка/фильтр по цене в каталоге идут в SQL, снэпшот корзины и Заказа — в PHP; два независимых места расчёта скидки рано или поздно разошлись бы по округлению. Half-up `bcadd(..., 0.5·10⁻ⁿ, scale)` даёт тот же результат, что `ROUND()` в MySQL для неотрицательных сумм — сверено на реальной БД (`dev-log.md` 20.09.2026) | Считать скидку только в PHP после выборки из БД — отклонено: фильтр `price_min`/`price_max` и сортировка `price_asc`/`price_desc` (`FR-CAT-004`) идут в SQL `WHERE`/`ORDER BY`, посчитать их после выборки уже нельзя |
| 2026-09-21 | ADR-042 | Новая таблица `addresses` (`user_id`, `title`, `city`, `street`, `house`, `apartment`, `comment`, `is_default`) — структурные поля, не одно `TEXT`-поле; «ровно один `is_default = 1`» на Покупателя обеспечивается в Model (`setDefaultAddress()` сбрасывает остальные перед установкой, `deleteAddress()` переназначает основной при удалении текущего), без `UNIQUE` на generated column, в отличие от `reserves.active_variant_id` (`ADR-007`) | `FR-ACC-002`, Таск 4 Фазы 7. Закрывает `Q-DEV-002` | `FR-ACC-002` прямо требует «поля определяются составом адреса», не одну строку; `is_default` — пользовательское предпочтение одного Покупателя, не ресурс, за который конкурируют параллельные запросы разных пользователей (в отличие от Выставочного образца) — конкурентная гонка на `UNIQUE`-constraint здесь избыточна, `dod-global.md` требует её только для случаев межпользовательской конкуренции за один и тот же ресурс | Одно поле `TEXT`, как `orders.delivery_address` — отклонено: книга адресов не отличалась бы тогда от истории введённых строк, нечего было бы редактировать/удалять по отдельности. `UNIQUE` на generated column по аналогии с `reserves` — отклонено как избыточное усложнение для однопользовательского предпочтения без гонки |
| 2026-09-21 | ADR-043 | СМС — журнал-заглушка вместо провайдера: новая таблица `sms_notifications` (`order_id`, `phone`, `event`, `message`), `Services/Sms.php::sendOrderSms()` только пишет строку в таблицу и в `app.log` (телефон замаскирован), реальный провайдер не подключается и не проектируется | `FR-NOTIF-001`, Таск 8 Фазы 7. Закрывает `Q-032` | Провайдер СМС в ТЗ не выбран (раздел 10.2, `Q-032`), демо-проект не нуждается в реальной отправке — тот же приём, что `/payment/stub` (`ADR-018`) и «Интеграции» (`ADR-039`); таблица нужна, чтобы Менеджер видел на странице Заказа, какие СМС «ушли» (`AC-05`) | Не заводить таблицу, только запись в лог — отклонено: `AC-05` требует показать историю уведомлений на странице Заказа в Панели управления, из файлового лога её не выбрать по `order_id` |
| 2026-09-21 | ADR-044 | Тело статической страницы (`content_pages.body`) — плоский текст с мини-разметкой (пустая строка = блок, `## ` = подзаголовок, `- ` = пункт списка), не HTML; рендер — одна чистая функция `renderContentBody()` (`src/Core/Content.php`), каждый текстовый фрагмент проходит через `e()`, результат — единственный вывод во View без повторного экранирования | `FR-ADM-003` правило 3 (редактирование ограничено содержимым — текст, фото — не структурой и вёрсткой), `FR-CNT-001…006`, Таск 1 Фазы 8 | Текст вводит Менеджер в Панели управления — по `php.md` это такой же внешний ввод, как форма Покупателя; трёх конструкций (абзац, подзаголовок, список) достаточно для страниц раздела 8.18, а чистая функция покрывается юнит-тестами, включая `<script>` в теле | HTML от Менеджера через `strip_tags` с whitelist тегов/атрибутов — отвергнуто: самописный санитайзер — известный класс XSS-ошибок (`javascript:` в `href`, `on*`-атрибуты, вложенные теги), а WYSIWYG-редактор из шаблона тянет лишний JS-плагин ради трёх конструкций |
| 2026-09-21 | ADR-045 | Новая таблица `settings` (key/value, `UNIQUE(key)`) заменяет константы `SHOP_PHONE`/`SHOP_PHONE_TEL`/`SHOP_WHATSAPP_URL` в `config.php`; читается через `setting()` (`Core/Settings.php`) с кэшем на один запрос (статическая переменная). `SHOP_PHONE_TEL` не переносится отдельным ключом — формат для `tel:` уже даёт существующий `normalizePhone()` (`Core/Validation.php`), новый `phoneToTel()` только делегирует ему, не дублируя regex | `FR-ADM-007` п. 1 и 4 в минимальном составе, Таск 2 Фазы 8 | Смена телефона/WhatsApp сегодня требует правки кода и деплоя — против духа `FR-ADM-003`/`FR-ADM-007` («без разработчика»); раздел «Настройки» (`admin`-only, первый такой маршрут, `Q-DEV-001`) даёт форму реквизитов и read-only блок «О системе» | Хранить реквизиты как отдельные колонки нескольких таблиц/файл `.env` — отклонено: `.env` не редактируется через UI и требует деплоя, а key/value с whitelist (`SETTING_KEYS`) проще, чем заводить по колонке на каждый будущий реквизит |
| 2026-09-21 | ADR-046 | Внеплановый редизайн `/about` по репорту пользователя (оригинал темы — Lorem ipsum/вымышленные лица/цифры): галерея (`about_gallery_images`, до 4 фото, `/admin/about-gallery`), блок статистики — демо-цифры (3 из 4 — плейсхолдер для портфолио, «18 месяцев гарантии» — реальный факт из ТЗ), цитата — один настоящий одобренный отзыв о магазине с фото (`reviews.photo_path`, добавляется в форме `/admin/reviews`), блок логотипов брендов не заводится (свой цех, не мультибренд) | Прямой запрос пользователя при показе скриншотов оригинала `about.html` | Демо-раздел портфолио (тот же приём, что `/payment/stub` `ADR-018`, «Каналы продаж» `ADR-039`) не должен показывать вымышленных людей/цифры без разбора — где есть настоящие данные (отзывы, гарантия), используются они, там где данных нет (стаж, число заказов) — цифры демо, без баннера «это демо» на публичной странице (в отличие от `/admin/sales-channels`, куда обычный посетитель не попадает) | Карусель из нескольких отзывов вместо одного — отклонено: одобренных отзывов с фото на старте нет вовсе (фото — новое поле), реальных данных под ротацию недостаточно, усложнение не оправдано; общая таблица `content_page_images` с `content_page_id` вместо отдельной `about_gallery_images` — отклонено: галерея нужна только одной странице, генерализация ради гипотетических будущих страниц (`general.md`: no premature abstraction) |
| 2026-09-22 | ADR-047 | Новая таблица `callback_requests` (`name`, `phone`, `comment`, `status ENUM('new','processed')`) для формы «Перезвоните мне» на `/contacts`, без `user_id` | `FR-CNT-001`, Таск 4 Фазы 8 | ТЗ не говорит, куда попадает заявка — решено в духе демо-раздела «Каналы продаж» (`ADR-039`): заявка с сайта видна там же, где Заказы (`/admin/callbacks`, Таск 5), а не только письмом. Без `user_id` — форму заполняют и гости, и авторизованные одним способом, привязка к аккаунту не даёт ничего, чего не даёт сам номер телефона в заявке | Только письмо через `sendMail()` — отклонено: на shared-хостинге `mail()` ненадёжен и такая заявка не видна в демо-Панели, только в почтовом ящике, который не показать на защите проекта |
| 2026-09-22 | ADR-048 | Провайдер ИИ выбирается по классу задачи, не по помощнику (`BR-AI-001` правило 3): класс «обезличенные данные» → Claude через OpenRouter, класс «пользовательский ввод» → YandexGPT — выбор задан только `.env` (`AI_PROVIDER_ANONYMOUS`/`AI_PROVIDER_USER_INPUT` и ключи), смена провайдера не требует правки кода. Класс без настроенного ключа = класс недоступен (`aiClassAvailable()` = `false`), запрос к провайдеру другого класса/юрисдикции не уходит молча. Единая точка вызова — `aiComplete()` (`src/Services/Ai/ai.php`), журнал каждого вызова — новая таблица `ai_requests`. **Отдельная гарантия сверх требования ТЗ:** сама модель не получает доступа к БД ни на чтение, ни на запись — `AiProvider::complete()` (`src/Services/Ai/AiProvider.php`) принимает только текст, файлы `src/Services/Ai/*` не подключают `Core/Database.php` и не содержат SQL; перевод стоимости в рубли и запись строки журнала — задача `Models/AiUsage.php`, а не провайдера | Таск 1 Фазы 9, прямое требование пользователя (2026-09-22) сверх текста `BR-AI-001` — модель должна быть архитектурно лишена возможности писать в БД, не только по соглашению между разработчиками | Отдельный read-only MySQL-пользователь для чтения данных под промпт — отклонено: на shared-хостинге создание второго пользователя БД — лишний ручной шаг в панели хостинга, а границу и так держит сама сигнатура интерфейса `AiProvider` (нет параметра для подключения к БД) |
| 2026-09-22 | ADR-049 | Разбор характеристик (`FR-AI-001`, Таск 2 Фазы 9): (1) `products.specs_status ENUM('pending','confirmed')` — очередь/допуск в подбор диалогом; (2) новая таблица `ai_spec_suggestions` (`target ENUM('spec','variant_material','variant_mechanism','color')`, `name`, `value`, `status ENUM('ok','needs_decision')`) — предложения до подтверждения Администратором; (3) «справочник допустимых значений Категории» (правило 3) — не новая таблица, а `DISTINCT` по уже существующим `product_variants.material`/`mechanism_type` и `variant_images.color` в пределах тех же Категорий (`getKnownSpecValues()`); (4) проверка «входит/не входит в справочник» применяется только к `variant_material`/`variant_mechanism`/`color` — у `target='spec'` (размер, форма и подобное) закрытого перечислимого словаря по Категории нет, поэтому такие предложения всегда `ok` | `FR-AI-001` правила 3, 4; `FR-AI-004` правило 1 (Товар без подтверждённых характеристик не участвует в подборе); закрывает часть `Q-DEV-005` (`.docs/tz-coverage.md`) | Пустая таблица-справочник на старте пометила бы «требует решения» вообще каждое предложение материала/цвета/механизма — бесполезно на 34 уже занесённых Товарах, где эти значения уже есть как данные каталога, а не как отдельно сопровождаемый справочник. Применение словарной проверки к `spec` дало бы обратный эффект: ширина/глубина — легитимно разные числа у каждого Товара, «непохоже на предыдущие» здесь не значит «ошибка» | Отдельная таблица `category_spec_dictionary` (категория → характеристика → допустимые значения), заполняемая Администратором вручную — отклонено пользователем при уточнении задачи (`+1 экран CRUD, ТЗ такого экрана не требует`); словарная проверка `needs_decision` для всех четырёх целей без исключения для `spec` — отклонено по итогам разбора реальных данных (см. выше) |
