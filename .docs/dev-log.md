# Dev Log

Журнал рабочих сессий. Дописывай в конец после каждой сессии.

---

## Формат записи

**Дата:** ДД.ММ.ГГГГ
**Что сделано:** ...
**Что следующее:** ...

---

**Дата:** 16.09.2026
**Что сделано:** Планирование Фазы 0 (`phase-init`). Снят запрет на jQuery — тема `00-input/design` целиком на нём (`ADR-026`); правило обновлено на месте в `CLAUDE.md` (стек, раздел JS), `.claude/rules/general.md`, `README.md`. Найдено: `database/install.php` отстаёт от `database.md` (`is_blocked`, `is_featured`, `content_pages`, `banners`); для `FR-AUTH-003/004` нужны таблицы `password_resets`/`remember_tokens` — войдут в Таск 1 Фазы 0.
**Что следующее:** Создать `.docs/phases/phase-0.md`, затем `task-init` для Таска 1.

---

**Дата:** 16.09.2026
**Что сделано:** Таск 1 Фазы 0 реализован и проверен запуском против
реальной БД (`mikhail700.beget.tech`, с подтверждения пользователя):
`database/install.php` дополнен `users.is_blocked`, `products.is_featured`
+ индекс, таблицами `remember_tokens`/`password_resets` (`FR-AUTH-003/004`,
`ADR-027`/`ADR-028`) и `content_pages`/`banners` (ранее пропущены при
переносе из `database.md`); `database/seed.php` создаёт первого
Администратора из `ADMIN_*` в `.env`, идемпотентно. `install.php`
дважды подряд без ошибок, итог 18 таблиц (в TASK.md изначально
ошибочно посчитано 16 — исправлено по факту). `seed.php` проверен на
успешную вставку, повторный запуск, короткий пароль, невалидный email,
отсутствующие переменные — во всех случаях корректное сообщение без
PHP-warning. `composer install` + `composer test` — 18/18 зелёных,
существующая логика не затронута.
**Что следующее:** Таск 2 Фазы 0 — базовый layout витрины и статика
темы (`task-init`).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 2 Фазы 0 реализован: статика темы перенесена в
`public/assets/` (без CDN; каталог `scss/` не переносился — исходники
не используются рантаймом), создан общий layout (`src/Views/layout/
header.php`, `footer.php`), компонент `src/Views/components/flash.php`
поверх существующих `getFlash()`, `HomeController` + `home.php`.
Проверено `php -S` + `curl`: `/` — 200 без записей в
`storage/logs/app.log`, несуществующий URL — 404, все ассеты отдают
200, внешних `http`-ссылок в шаблонах нет. Не проверено (нет браузера
в сессии): реальная консоль JS, работа off-canvas меню, вёрстка на
320px — отмечено в TASK.md как требующее ручной проверки.

По ходу ревью пользователем найдено и поправлено: длинные русские
подписи меню переносились на 2 строки, заголовки карточек преимуществ
были слишком крупными (иконки демо-темы вдобавок смыслово не совпадали
с подписями — известное расхождение плейсхолдера, не исправлялось) —
добавлен свой `public/assets/css/app.css` (по аналогии с `app.js`,
тему не трогаем) с точечными переопределениями размеров шрифта. Также
по требованию пользователя блок Call-To-Action заменён на реальный
слайдер темы (`.slider-section`/`.slider-shape`/Swiper, те же картинки
`slider-item-1/2/3.png`, русский текст про ДомФорм) с высотой `100vh`
(единственное отличие от родной темы, где `.single-slider` — `950px` +
адаптивные брейкпоинты; фон `#e9e1f7` и `.slider-shape` уже совпадали
1:1 в скопированном `style.css`) и уменьшенным заголовком слайда
(`80px` → `56px`, на `≤767px` — `32px`).
**Что следующее:** Ручная проверка Таска 2 в браузере (консоль, mobile
menu, 320px) — после неё Таск 2 → ✅ Завершён и `task-init` для Таска 3
Фазы 0 (регистрация/вход/выход).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 3 Фазы 0 реализован: `src/Core/Validation.php`
(`validateEmail`/`normalizePhone`/`validatePhone`/`validatePassword`,
чистые функции, 13 unit-тестов), `src/Models/User.php`
(`findUserByEmail`/`findUserById`/`createUser` — последняя перехватывает
дубликат `UNIQUE(email)` по SQLSTATE 23000 / MySQL 1062 как проигрыш
гонки регистрации, а не системную ошибку, по аналогии с паттерном
`reserves` из `database.md`), `AuthController`
(`showLogin`/`login`/`showRegister`/`register`/`logout`),
`src/Views/auth/{login,register}.php` по вёрстке темы (`.login-register-
wrapper`/`.single-form`, `is-invalid`/`.invalid-feedback` — уже в
бандле `style.css`, новых стилей не добавлялось; блок `page-banner-
section` с декоративным фоном не переносился — в дизайне он на inline
`style="background-image:..."`, что запрещено `general.md`), `header.php`
(десктоп + мобильная копия дропдауна) показывает имя пользователя
(добавлено в `$_SESSION['user_name']` при логине) и форму «Выход»
(POST + `csrfField()`, т.к. `/logout` только POST) вместо статичных
ссылок. `redirectIfAuthenticated()` → `/`. Регистрация не логинит
автоматически — редиректит на `/login` с флэшем (`regenerateSession()`
по правилам вызывается только после успешного входа, не регистрации).
Ошибка «занятый email» при регистрации и «неверный пароль»/«нет
такого email»/`is_blocked` при входе намеренно используют один и тот же
текст (`AuthController::AUTH_ERROR`) — не раскрывают существование
аккаунта.

Проверено `php -S` + серией `curl` против реальной БД
(`mikhail700.beget.tech`, тестовые записи удалены после проверки):
пустая форма регистрации → 4 подсвеченных поля; частичная ошибка →
`name`/`email` сохранены в форме, только `phone`/`password` с
`is-invalid` и текстом ошибки; успешная регистрация с кириллическим
именем (после того как выяснилось, что `curl --data-urlencode` в этом
Bash-окружении Windows ломает кириллицу в аргументах — не баг
приложения, обошёл через файл с заранее собранным `http_build_query`);
вход с верным паролем → шапка показывает имя + форму «Выход»; вход с
неверным паролем и с несуществующим email → идентичный текст ошибки;
повторная регистрация на тот же email → тот же текст; `is_blocked=1` →
тот же текст даже с верным паролем; 6-я попытка входа за минуту
отклонена `tooManyAttempts('login', 5, 60)` с отдельным сообщением
до обращения к БД; все три POST-обработчика без `_csrf` → 419; выход →
шапка возвращается к «Вход»/«Регистрация». `sanitizeLogContext`
отдельно проверен: подставленный `password` в контекст `logWarning()`
маскируется в `[REDACTED]` (боевой `APP_LOG_LEVEL=error` в `.env`
фильтрует уровень `warning`, поэтому неудачные попытки входа не пишутся
в `storage/logs/app.log` при текущей конфигурации — код вызывает
`logWarning()` корректно, это настройка окружения, не баг). `composer
test` — 32/32 зелёных.

Не проверено (нет браузера в сессии): консоль JS, вёрстка форм на
320px — как и в Таске 2, отмечено в TASK.md.
**Что следующее:** Ручная проверка Таска 3 в браузере → Таск 3 → ✅
Завершён, затем `task-init` для Таска 4 (роли, `requireRole()`,
Remember me).

---

**Дата:** 17.09.2026
**Что сделано:** Ручная проверка Таска 3 в браузере пользователем нашла
три бага в общем layout (не в Task 3 напрямую, но всплыли из-за
flash-сообщений после логина/логаута) — все поправлены точечно в
`app.css`/`app.js`, тему не трогали:
1. `header-area`/`header-mobile` темы всегда `position: absolute`
   (оверлей поверх верха страницы, рассчитан на высокий hero под собой);
   `flash.php` и формы `login.php`/`register.php` рендерились без
   отступа и прятались под хедером — а хедер, оставаясь поверх
   (`z-index: 99`), ещё и перехватывал клики по кнопке закрытия
   уведомления. Сначала попробовал зашить отступ статичными числами из
   `.page-banner-content` темы — не подошло (это внутренний отступ
   текста внутри готового 500px-блока баннера, не высота хедера).
   Итоговое решение — класс `.page-content-offset` + `app.js` меряет
   реальный `outerHeight()` видимого сейчас хедера (`:visible`,
   desktop/mobile разные) и подставляет как `padding-top`, никаких
   магических чисел на брейкпоинт.
2. `flash.php` — уведомления стали закрываемыми (`alert-dismissible` +
   `data-bs-dismiss="alert"`, нативный Bootstrap 5, без своего JS) —
   раньше висели до перезагрузки страницы.
3. Мобильный дропдаун-профиль в `header.php` остался со статичными
   ссылками «Вход»/«Регистрация» даже у авторизованного — при переносе
   логики на реальный статус входа `replace_all` задел только
   десктопный блок (разный отступ у копий). Мобильный блок приведён к
   тому же виду (имя + форма «Выход»), что и десктопный.
4. `main.js` темы переключает класс `.sticky` (белый фон хедера) только
   по событию `scroll` — если страница открывается уже с ненулевым
   скроллом, класс мог «залипнуть» навсегда, событие `scroll` больше не
   срабатывает. `app.js` синхронизирует состояние сразу при загрузке
   той же логикой.

Все фиксы проверены через `curl` против реальной БД проекта — детали
и скрипты проверки не сохранялись (интерактивная сессия), но `composer
test` (32/32) прогонялся после каждого фикса.
**Что следующее:** `task-init` для Таска 4 (роли, `requireRole()`,
Remember me).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 4 Фазы 0 реализован: `currentUser()`/
`roleAllowed()`/`requireRole()` в `functions.php` — читают
`id`/`name`/`role` только из `$_SESSION` (кладутся туда при логине и
при авто-входе по remember-cookie), без похода в БД на каждый защищённый
маршрут; `roleAllowed()` вынесена отдельной чистой функцией специально
ради unit-теста (`requireRole()` сама делает `redirect()`/`exit`, не
тестируется напрямую — тот же паттерн, что уже был с `requireAuth()`).
`src/Models/RememberToken.php` — `createRememberToken`/
`findRememberToken`/`deleteRememberTokens`, пара selector(24 hex)/
validator(64 hex) как в `password_resets`, в БД только
`sha256(validator)`. `AuthController::login()` — чекбокс «Запомнить
меня» → cookie `selector:validator` (`httponly`/`samesite=Lax`/`secure`
в проде, 30 дней); `logout()` — чистит все remember-токены
пользователя (не только текущего устройства — осознанно самый
безопасный дефолт) и cookie. `attemptRememberLogin()` вызывается в
`public/index.php` до `dispatch()`, сама логика — в `functions.php`
(`index.php` остаётся front controller-ом). `AdminController` + `admin/
index.php` — заглушка под `requireRole(['manager', 'admin'])`,
реальная функциональность — другая фаза проекта (Фаза 4), не этот
таск.

По ходу таска добавлено то, что не было в исходном плане `phase-0.md`
Таска 4: раз появляется первая cookie за пределами сессии
(remember-token), сделали информационный (не блокирующий) баннер о
cookie — `src/Views/components/cookie-notice.php`, подключён в
`footer.php` (общий для всех страниц), показ/скрытие и
`localStorage`-флаг — в `app.js`, минимальные BEM-стили — в `app.css`.
Ссылки на страницу политики cookie нет — `content_pages` из
`database.md` ещё не заполнены (другая фаза).

Проверено `php -S` + серией `curl` против реальной БД
(`mikhail700.beget.tech`, три тестовых аккаунта по ролям + временная
remember-запись, все удалены после проверки): `/admin` — 403 у
`customer`, 200 у `manager`/`admin`, 302 → `/login` у гостя; логин с
чекбоксом → cookie-jar без `PHPSESSID` (эмуляция закрытия браузера) →
`/` всё ещё показывает имя в шапке, роль восстановлена (`/admin` даёт
тот же 403/200); без чекбокса cookie не ставится и строка в
`remember_tokens` не создаётся; `Set-Cookie` подтверждает `HttpOnly`/
`SameSite=Lax`; выход → `Set-Cookie: remember_token=deleted` +
`DELETE` всех токенов пользователя (проверено SQL); протухшая
(`expires_at` в прошлом), несуществующая и подделанная (неверный
validator) cookie — все три 200 без ошибок в `app.log`, cookie
очищается ответом сервера. `composer test` — 40/40.

Не проверено (нет браузера в сессии): клик по кнопке «Понятно» у
cookie-баннера и то, что `localStorage`-флаг реально не даёт ему
показаться повторно — верстка/JS на месте (баннер и `app.js` есть в
HTML-ответе), но сам клик руками не воспроизвести без браузера;
вёрстка на 320px — как и в предыдущих тасках.
**Что следующее:** Ручная проверка Таска 4 в браузере (клик по
cookie-баннеру, 320px) → Таск 4 → ✅ Завершён, затем `task-init` для
Таска 5 (восстановление пароля) — последний таск Фазы 0.

---

**Дата:** 17.09.2026
**Что сделано:** Пользователь при ручной проверке нашёл, что после
входа Администратор/Менеджер попадал на `/`, а не в `/admin` — в
`phase-0.md` («Решения фазы») это было явно зафиксировано ещё до Таска
4, но при реализации `AuthController::login()` упустил и редиректил
всех на `/` без учёта роли. Поправлено: `homeUrlForRole(?string $role):
string` в `functions.php` (customer → `/`, manager/admin → `/admin`),
переиспользуется в `login()` и в `redirectIfAuthenticated()` (раньше
она тоже не смотрела на роль — GET `/login` уже авторизованным
Администратором тоже уводил на `/`). Проверено `curl` тремя ролями
против реальной БД (тестовые аккаунты удалены после проверки): вход —
customer → `/`, manager/admin → `/admin`; повторный `GET /login` уже
залогиненным — тот же результат. `composer test` — 40/40, без
регрессий.
**Что следующее:** Ручная проверка Таска 4 в браузере (клик по
cookie-баннеру, 320px) → Таск 4 → ✅ Завершён, затем `task-init` для
Таска 5.

---

**Дата:** 17.09.2026
**Что сделано:** Два мелких фикса по итогам ручного ревью пользователя
(не отдельные таски — точечные правки в файлах, уже тронутых Тасками
3–4):
1. Иконка аватара в шапке у авторизованного пользователя визуально не
   отличалась от гостя. `header.php` — класс `action--active` на
   иконке при `$isLoggedIn` (десктоп + мобильная копия); `app.css` —
   тот же акцентный `#f2a100`, что тема уже использует для активного
   пункта меню/hover иконок. Набор иконок темы (`pe-7s`, только
   контурные) не даёт «залитого» варианта под замену картинки — если
   ожидалась именно другая иконка, а не цвет, нужен свой SVG.
2. После входа/выхода шапка выглядела «залипшей» белой даже после
   закрытия уведомления крестиком — воспроизводилось только сразу
   после редиректа, полная перезагрузка чинила. Причина не в `.sticky`
   (как в прошлом похожем баге): `flash.php` оборачивает уведомление в
   `.page-content-offset` (см. фикс от 17.09.2026 выше), но Bootstrap
   при закрытии крестиком удаляет только сам `.alert` — пустая обёртка
   с её `padding-top` остаётся, и это пустое белое место выглядит как
   «хедер не стал прозрачным». После полной перезагрузки флеш уже не
   рендерится (`getFlash()` одноразовый), поэтому обёртки нет вовсе и
   всё нормально — отсюда путаница «чинится только reload'ом». Фикс —
   `app.js` слушает `close.bs.alert` и удаляет опустевшую
   `.page-content-offset` следом за алертом.

Оба фикса проверены `curl`/статическим анализом HTML-ответа, сам клик
по крестику и визуальный цвет иконки — нужен реальный браузер (нет в
сессии). `composer test` — 40/40.
**Что следующее:** `task-init` для Таска 5 (восстановление пароля).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 5 Фазы 0 реализован — последний таск фазы.
`src/Models/PasswordReset.php` — `createPasswordReset()` (перед
вставкой новой записи помечает `used_at` у прежних неиспользованных
токенов пользователя — причина `INDEX(user_id)` в `database.md`,
иначе старые ссылки из более ранних писем продолжали бы работать),
`findValidPasswordReset()` (проверка истечения через `strtotime()`/
`time()` в PHP, по образцу `attemptRememberLogin()` из Таска 4, не
`SQL NOW()`), `markPasswordResetUsed()`. `User.php` —
`updateUserPasswordHash()`. `src/Services/Mailer.php` —
`sendMail()`/`renderEmailBody()`: `match` по `env('MAIL_DRIVER')`,
`log` пишет тело письма через `logInfo()`, `mail` — через PHP `mail()`
с логированием неудачной отправки (`logError()`, без деталей
gateway-уровня в тексте, как требует `php.md` для платёжных ошибок —
тот же принцип применён и здесь). `AuthController` — `forgot()`
всегда отвечает одним и тем же генерик-текстом независимо от
существования email; `reset()` использует ту же `validatePassword()`,
что регистрация, и переиспользует `deleteRememberTokens()` Таска 4.
Добавлена рабочая ссылка «Забыли пароль?» на `login.php` — сознательно
отложенная в Таске 3.

Проверено `php -S` + `curl` против реальной БД (тестовый пользователь
удалён после проверки, каскадное удаление `password_resets` при
удалении `users` подтверждено): одинаковый флэш для существующего и
несуществующего email; прямым вызовом `createPasswordReset()` (в обход
`APP_LOG_LEVEL=error`, который фильтрует уровень `info` — та же
особенность окружения, что с `logWarning()` в Таске 3) подтверждено
корректное содержимое письма и аннулирование прежнего токена при
повторном запросе (первый токен валиден сразу после создания,
становится невалидным после второго запроса); полный цикл через HTTP:
короткий пароль отклонён с `is-invalid`, валидный пароль — 302 на
`/login`, вход новым паролем — успех, старым — генерик-ошибка,
повторное открытие использованного токена и обращение к
несуществующему — редирект на `/forgot-password`; 4-й быстрый запрос
`/forgot-password` за минуту отклонён `tooManyAttempts('forgot', 3,
60)`; оба POST-эндпоинта без `_csrf` — 419. `composer test` — 40/40
(новых тестов не добавлял — вся логика таска трогает БД/IO, по
`php.md` не юнит-тестируется, тот же прецедент что `User.php`/
`RememberToken.php`).

Фаза 0 закрыта: `.docs/phases/phase-0.md` (статус фазы и Таска 5),
`.docs/phases/_status.md` (строка Фазы 0 → ✅ Завершена),
`.docs/tz-coverage.md` (`FR-AUTH-001…005` — реализовано).
**Что следующее:** Ручная проверка Таска 5 в браузере → Фаза 0
полностью закрыта, затем `task-init` для Фазы 1 (каталог и карточка
товара) — после того как для неё будет создан `.docs/phases/phase-1.md`.

---

**Дата:** 17.09.2026
**Что сделано:** Таск 1 Фазы 1 реализован и проверен против реальной БД
(`mikhail700.beget.tech`, с подтверждения пользователя): `categories`
дополнена `description TEXT NULL` (`ADR-029`), новая таблица
`product_specs` (`ADR-030`); `database/seed-catalog.php` — 6 категорий в
2 уровня, 12 товаров (17 Вариантов, 19 фото, 26 характеристик),
идемпотентно. При первом прогоне против уже развёрнутой (Таском 1
Фазы 0) БД поймана нетривиальность: MySQL 8.4 (Beget) не поддерживает
`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` — синтаксическая ошибка;
переписано на проверку через `information_schema.COLUMNS` перед `ALTER`.
`install.php` и `seed-catalog.php` дважды подряд без ошибок и дублей
(сверено `COUNT(*)` по всем 6 таблицам catalog до/после); данные
включают Вариант-образец, цвет `is_swatch=1` без реального фото, товар
в 2 категориях с ровно одним `is_primary`, Варианты с
`mechanism_type IS NULL`, заполненные `description` у категорий,
неактивный товар — все проверены прямыми SQL-запросами. `composer test`
— 40/40 зелёных (без новых тестов — вся логика таска работает с БД).
**Что следующее:** `task-init` для Таска 2 Фазы 1 — листинг каталога
(категории, сортировка, пагинация, мини-карточка).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 2 Фазы 1 реализован: `src/Models/Category.php`
(`getCategoryTree()` — плоский список из БД собирается в root=>children
в PHP, не рекурсивным SQL; `findCategoryBySlug()`, `getCategoryPath()`),
`src/Models/Product.php` (`getCatalogProducts()`/`countCatalogProducts()`
— цена/фото/материал/цвет мини-карточки берутся от самого дешёвого
активного Варианта товара батч-запросом с `ROW_NUMBER() OVER (PARTITION
BY ...)`, MySQL 8-специфично, без N+1 на страницу), `src/Core/
Pagination.php` (`buildPagination()` + `buildPaginationUrl()`, обе чистые
функции, `tests/Unit/PaginationTest.php` — 9 тестов), `CatalogController`
(`index()`/`category()`, 404 через новую `abort404()` в `ErrorHandlers.php`
— тот же ответ, что и `Router::dispatch()` на неизвестный маршрут),
`catalog/index.php` + компоненты `product-card`/`breadcrumbs`/`pagination`
по `shop-grid-left-sidebar.html` (без `page-banner-section` — тот же
inline-style запрет, что и в Таске 3 Фазы 0), меню «Каталог» в
`header.php` (десктоп + off-canvas) — переиспользован неиспользуемый
в кураторской теме `.mega-sub-menu`/`.menu-title`/`.menu-item` CSS
(есть в `style.css`, не было ни в одном HTML темы) вместо баннеров —
точно то же назначение, что нужно для колонок категорий, своего CSS не
добавлял. Заодно: `formatPrice()` в `functions.php` (без float, `(int)`
на DECIMAL-строку из PDO) и `CATALOG_PER_PAGE=12` в `config.php`
(`phase-1.md`, «Решения фазы»).

Цена/материал/цвет мини-карточки — решение не из явного текста таска:
«цвет текстом» реализован как цвет главного фото самого дешёвого
Варианта (не список всех различных цветов товара) — так мини-карточка
остаётся однострочной, без doп. запроса на `GROUP_CONCAT`.

Проверено `php -S` + `curl` против реальной БД (`mikhail700.beget.tech`):
`/catalog` и все 6 `/catalog/{slug}` — 200, неизвестный slug — 404;
11 активных из 12 сидов (неактивный «Шкаф «Классик»» скрыт); товар
«Осло» виден и в `/catalog/divany`, и в `/catalog/krovati`; хлебные
крошки на `/catalog/divany` — Главная → Мягкая мебель → Диваны;
`?sort=price_asc` сохраняет выбор в `<select>`; ни «в наличии», ни
`pe-7s-like` нет в выдаче; меню «Каталог» отдаёт все 6 категорий.
Пагинация с реальными 11 товарами умещается на одну страницу — блок
пагинации корректно скрывается (`total_pages<=1`); отдельно проверена
многостраничность и `page=999` временным понижением
`CATALOG_PER_PAGE` до 3 (4 страницы, `page=999` откатывается на
последнюю без ошибки) — константа возвращена на 12 после проверки,
`git diff` подтверждён чистым. `storage/logs/app.log` не создан за всю
сессию — ни одной PHP-ошибки/warning'а. `composer test` — 49/49
(40 + 9 новых `PaginationTest`).

Не проверено (нет браузера в сессии): визуальная раскладка mega-меню
категорий и мобильный аккордеон off-canvas (JS-логика уже в `main.js`
из Таска 2 Фазы 0, но клик руками не воспроизвести); вёрстка на 320px.
Замечено, но не в скоупе: слайдер на Главной (`home.php`) всё ещё
ссылается на каталог через `href="#"` — можно поправить на `/catalog`
отдельным точечным изменением, когда попадётся в скоуп.
**Что следующее:** Ручная проверка Таска 2 в браузере (mega-меню,
320px) → Таск 2 → ✅ Завершён, затем `task-init` для Таска 3 (фильтры
без перезагрузки, пустое состояние, плитка/список).

---

**Дата:** 17.09.2026
**Что сделано:** Два мелких фикса по скриншоту ручного ревью
пользователя `/catalog` (не отдельный таск — точечные правки поверх
Таска 2):
1. Счётчик «N из M товаров» — тема капсит весь `<p>` через
   `text-transform: uppercase` (ломает читаемость русского текста:
   «из»/«товаров» превращались в «ИЗ»/«ТОВАРОВ») и не увеличивала
   цифры. Цифры обёрнуты в `<span>` (`catalog/index.php`) — тот же
   `<span>`, что и в оригинальной разметке темы, уже даёт жёлтый акцент
   из `style.css`; в `app.css` — `text-transform: none` на `p` и
   `font-size: 1.2em` на `span`.
2. Фото карточек каталога — вместо фото демо-темы из сидов (не
   соответствуют русским названиям товаров: «Шкаф-купе «Гардероб»»
   с случайным «product-01.jpg» и т.п., визуально вводит в
   заблуждение) везде подставлена единая заглушка
   `public/assets/images/placeholder-product.svg` (серый прямоугольник
   270×303 — размер запрошен пользователем). `Product.php`/
   `image_path` не трогал — Модель по-прежнему отдаёт реальный путь,
   заглушка подставляется на уровне `product-card.php`, чтобы
   вернуть настоящие фото было одной строкой, когда появится
   фотосъёмка (Фаза 4, `FR-ADM-001`).

Проверено `curl` против реальной БД: `<span>11</span> из <span>11</span>
товаров` в разметке, `placeholder-product.svg` отдаётся 200 и
подставлен во все карточки. `composer test` — 49/49, `storage/logs/
app.log` не создан.
**Что следующее:** Ручная проверка Таска 2 в браузере → Таск 2 →
✅ Завершён, затем `task-init` для Таска 3.

---

**Дата:** 17.09.2026
**Что сделано:** Правка предыдущего фикса — пользователь уточнил, что
имел в виду не отдельную серую SVG-заглушку фиксированного размера
270×303, а фото карточек из существующего набора темы
`assets/images/product/product-01…13.jpg`, и размер должен остаться
естественным (как задаёт тема — `width: 100%`, высота по пропорциям
файла), без хардкода `width`/`height` в `<img>`.
`product-card.php` — заглушка выбирается детерминированно по id товара
(`product-{((id-1) % 13) + 1}.jpg`, не реальный `$product['image_path']`
из БД — тот же принцип, что и раньше: Модель не трогал, подмена на
уровне компонента); `placeholder-product.svg` удалён как неиспользуемый.

При правке поймал баг до продакшена: первая версия объявляла
`const PRODUCT_CARD_PLACEHOLDER_COUNT` прямо в `product-card.php` —
этот файл подключается `include` внутри `foreach` в `catalog/index.php`
один раз на каждый товар, второй `include` за проход упал бы фатальной
ошибкой «Cannot redeclare constant». Заменено на локальную переменную
без объявления константы в файле.

Проверено `curl` против реальной БД: 11 карточек — 11 разных
`product-XX.jpg` без повторов и без фатала; `width="270"` в разметке
больше нет. `composer test` — 49/49, `storage/logs/app.log` не создан.
**Что следующее:** Ручная проверка Таска 2 в браузере → Таск 2 →
✅ Завершён, затем `task-init` для Таска 3.

---

**Дата:** 17.09.2026
**Что сделано:** Таск 3 Фазы 1 реализован — фильтры каталога без
перезагрузки, пустое состояние, плитка/список.

`src/Core/CatalogFilters.php` — `normalizeCatalogFilters()` (чистая,
принимает уже готовый массив запроса, не сама `$_GET`): whitelist
сортировки, `category`/`color` — int[]/string[] (принимают и скаляр, и
массив из чекбоксов), границы цены со swap при перепутанных
min/max, `in_stock` только по строке `'1'`; `buildCatalogQueryString()`
— обратная сериализация без значений по умолчанию (чистый URL) и без
`page` (её добавляет пагинация). 14 новых тестов в
`tests/Unit/CatalogFiltersTest.php`.

`src/Models/Product.php` — `getCatalogProducts()`/`countCatalogProducts()`
переведены на общий `buildCatalogFilterConditions()` (список и счётчик
не могут разойтись по условиям): каждый фильтр — `EXISTS`, Товар
отбирается, если хотя бы один активный Вариант ему удовлетворяет (тот
же принцип, что уже был для цены в `phase-1.md`), не «все Варианты
сразу». `getFilterOptions(?int $categoryId)` — min/max цена и список
цветов в том же скоупе (категория), что и сам листинг.

`CatalogController` — категория чекбоксов в сайдбаре показывается
только на `/catalog`; на `/catalog/{slug}` категория берётся из пути
безусловно, чекбоксы отключены совсем (осознанное упрощение — иначе
пришлось бы различать «фильтр не тронут» и «пользователь снял
единственную галочку» у чекбоксов в обычной GET-форме, чего не требует
ни один пункт DoD). Фрагмент по `X-Requested-With: fetch` (новая
`isFetchRequest()` в `Request.php`, по образцу `isPost()`/`isGet()`) —
`render('components/catalog-grid', ...)` напрямую, без обёрточного
view: `render()` не ограничивает подпапку, отдельный шаблон не нужен.

Вёрстка: `catalog-sidebar.php` (статичная часть, не подменяется
fetch'ем) — категория/наличие/цвет чекбоксами, цена — свой
`#catalog-price-slider` (не тема-шный `#price-range`, у него уже есть
хардкод min/max/from/to в `main.js`, вешавшийся бы на любую страницу
сайта). `catalog-grid.php` (фрагмент: счётчик+сортировка+вид+сетка/
список+пагинация или `empty-state.php`) внутри `#catalog-results`.
`product-card.php` — второй режим разметки (`$viewMode`, общий
`product-meta` через `ob_start()`, чтобы не дублировать разметку формы
корзины) — без этого плитка/список (сама часть Scope Таска 3) нечем
было бы наполнить: тема использует РАЗНЫЙ HTML для плитки
(`single-product`) и списка (`single-product-02.product-list`), не
CSS-класс поверх одной разметки. Обе раскладки рендерятся в DOM
одновременно (два `tab-pane` на Bootstrap `data-bs-toggle="tab"` —
переключение работает нативно, свой JS нужен только для восстановления
сохранённого вида после `sessionStorage`).

`app.js` — `fetch` + `pushState`/`popstate`, сортировка `<select>`
живёт внутри фрагмента, связана с сайдбар-формой через HTML `form=`
(для no-JS), для JS-пути URL собирается вручную (`FormData` не видит
полей вне DOM-поддерева формы). Плитка/список — `sessionStorage` +
`bootstrap.Tab` API (нативный, не jQuery-обёртка — `general.md`);
`shown.bs.tab`/`data-bs-toggle="tab"` у Bootstrap 5 делегированы на
`document`, поэтому не требуют повторной инициализации после каждой
подмены `#catalog-results`. `[aria-busy="true"]` — состояние загрузки
(`app.css`, тускнеет+не кликается на время `fetch`).

По ходу найдена и закрыта дыра в `dod-global.md`: фильтр по цвету — новый
запрос с `WHERE color IN (...)`, индекса на этой колонке не было.
Добавлен `INDEX(color)` на `variant_images` (`ADR-031`,
`database/install.php` — идемпотентно через `information_schema`, тот
же приём, что `categories.description` в Таске 1).

Проверено `curl` против реальной БД (`mikhail700.beget.tech`):
`in_stock=1` → 1 из 1 (Вариант-образец); `price_min=40000&price_max=
50000` находит `divan-verona` (Варианты 30000/45000 — сработал именно
EXISTS-по-любому-Варианту); `category[]=3` на `/catalog` → 3 (Кресла),
на `/catalog/divany` виджет категории отсутствует (0 чекбоксов);
пустая комбинация → «Ничего не найдено» + `catalog-reset-link`
корректно ведёт на текущий путь без query (`/catalog` или
`/catalog/divany`); фрагмент по `X-Requested-With: fetch` — без
`<!DOCTYPE html>`, обычный GET — полная страница; пагинация с
временно применённым `CATALOG_PER_PAGE=3` сохраняет `sort`+`price_min`
во всех номерах страниц (константа возвращена на 12,
`git diff --stat` подтверждён чистым). `node --check app.js` —
синтаксис ок. `composer test` — 63/63 (49 + 14 новых).
`storage/logs/app.log` не создан за всю сессию — ни одной ошибки.

Не проверено (нет браузера в сессии): сам клик по фильтрам/`fetch`-
подмена/`pushState`+«назад»/переключение плитка-список визуально —
серверная часть каждого из них проверена по отдельности через `curl`,
но живого клиентского прогона не было.
**Что следующее:** Ручная проверка Таска 3 в браузере (фильтры, назад/
вперёд, плитка/список, 320px) → Таск 3 → ✅ Завершён, затем `task-init`
для Таска 4 (карточка товара с выбором Варианта).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 4 Фазы 1 реализован — карточка товара с выбором
Варианта.

`src/Models/Product.php` — `findProductBySlug()` сразу джойнит
primary-категорию (`is_primary = 1`, ровно одна на Товар — гарантия
сидов Таска 1): она нужна и хлебным крошкам, и «Похожим товарам», не
запрашивать же дважды. Неактивный Товар не находится вовсе (`p.is_active
= 1` прямо в `WHERE`) — тот же результат, что несуществующий slug,
контроллеру не нужно различать два повода для 404. `getProductVariants()`/
`getVariantImages()`/`getProductSpecs()` — простые выборки.
`getRelatedProducts()` возвращает **тот же формат строки**, что
`getCatalogProducts()` (переиспользует уже существующий
`attachCheapestVariant()`) — значит «Похожие товары» рендерятся тем же
`product-card.php`, второго шаблона мини-карточки не потребовалось.

`ProductController::show()` — 404 через уже существующую `abort404()`
(Таск 2). Хлебные крошки собираются переиспользованием
`getCategoryPath()` (Таск 2): из primary-категории строится
минимальный массив категории и в конец добавляется сам товар — тот же
компонент `breadcrumbs.php`, что и в каталоге, без изменений.

`variant-selector.php` — данные всех Вариантов и их фото в
`data-variants` (JSON, `JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|
JSON_HEX_QUOT` — требование DoD), переключение полностью на клиенте
(`app.js`, `initProductVariantSelector()`). Кнопки материала рендерятся,
только если Вариантов больше одного; при единственном Варианте он
предвыбран сразу (`data-preselected`) — клиентская блокировка «В
корзину» включается только когда `data-preselected` пуст.

Осознанное упрощение (не в исходном плане Таска, но нужно было решить
по ходу): swiper-галерея с миниатюрами из темы (`product-details-
affiliate.html`) заменена одним `<img id="product-main-image">`,
который JS просто подменяет по `src`/`alt`. На сидах Таска 1 у каждого
сочетания Вариант+цвет ровно одна фотография — полноценная карусель с
несколькими кадрами на один выбор показывать было бы нечего, а
переинициализация Swiper при каждой смене Варианта/цвета — заметная
JS-сложность без браузера в сессии для проверки. `zoom.js` тоже не
подключался по той же причине.

По ходу поймал и сразу поправил мелкий баг: изначально блок «Механизм»
рендерился пустым `<p></p>` при `mechanism_type = NULL` (например,
«Кресло «Комфорт»») — пробовал скрыть через CSS `:empty`, но
`:empty` не сработал бы из-за пробельного текстового узла между
тегами в шаблоне (перенос строки вокруг `<?= ?>`). Заменено на явный
атрибут `hidden` от PHP при первой отрисовке и от JS при переключении
Варианта — не полагается на побочный эффект форматирования шаблона.

Инфраструктурная находка, не баг кода: первые прогоны `curl` падали
`500` с `PDOException: MySQL server has gone away` — оказалось, с
предыдущих тасков этой сессии осталось 8 фоновых процессов `php -S`,
все на одном порту 8099, конкурировавших за соединение с БД
(`beget.tech`, вероятно, ограничивает параллельные подключения с
одного адреса/логина). Все лишние процессы остановлены (`kill -9`),
после чего все запросы прошли штатно; за это себе на будущее — глушить
dev-сервер `pkill` по фактическому имени процесса (`php`), а не по
командной строке `php -S localhost:PORT` (на Windows/Git Bash `ps aux`
её не показывает, поэтому прежний `pkill -f "php -S localhost:8099"` в
предыдущих тасках тихо не находил цель и ничего не останавливал).

Проверено `curl` против реальной БД (`mikhail700.beget.tech`):
`/product/divan-verona` и `/product/divan-milan` — 200; `/product/
does-not-exist` и `/product/shkaf-klassik` (неактивный) — 404;
хлебные крошки — Главная → Мягкая мебель → Диваны → Диван «Верона»;
цена на странице — первого (самого дешёвого) Варианта; JSON Вариантов
содержит оба флага `is_showroom_sample` (false/true) и swatch-цвет
«Коричневый» с `is_swatch: true` и валидным путём
(`product-details/product-details-1.jpg` из сидов Таска 1); таблица
характеристик — есть у `divan-verona`, отсутствует (0 совпадений) у
`kreslo-kachalka-uyut` (без `product_specs`); «Похожие товары» на
`divan-verona` — три других дивана, без самого товара и без
неактивных; `grep -ic "buy now"` → 0; `data-preselected` пуст для
многовариантных товаров и заполнен для товара с одним Вариантом
(`kreslo-komfort`); в `data-variants` нет сырых `<script>`/`</script>`
(HEX-флаги `json_encode` работают). `composer test` — 63/63 (без новых
тестов — вся логика таска идёт через БД/HTTP, по `php.md` не
юнит-тестируется). `storage/logs/app.log` не прирастал новыми
записями после устранения конфликта портов.

Не проверено (нет браузера в сессии): сам клик по кнопкам материала/
цвета и визуальная смена фото/цены/текста, блокировка сабмита формы
без выбора Варианта — код вычитан и синтаксически проверен
(`node --check app.js`), но не воспроизведён руками.
**Что следующее:** Ручная проверка Таска 4 в браузере (клик по
Варианту/цвету, блокировка «В корзину», 320px) → Таск 4 → ✅ Завершён,
затем `task-init` для Таска 5 (поиск с подсказками) — последний таск
Фазы 1.

---

**Дата:** 17.09.2026
**Что сделано:** Фикс по скриншоту ручного ревью пользователя —
главное фото карточки товара рендерилось крошечным (270×303px) в
большой колонке, вся страница выглядела «не по теме». Причина — не
CSS-баг, а сочетание двух фактов: (1) сами файлы `assets/images/
product/product-XX.jpg`, которые Таск 1 использует как пути в сидах
`variant_images.path`, физически 270×303px — так их поставляет сама
купленная тема (тот же набор уже используется в мини-карточках
каталога с Таска 2, просто там это не так заметно на маленькой
карточке); (2) на карточке товара фото было обёрнуто в Bootstrap
`.img-fluid` (`max-width: 100%`), который уменьшает большие картинки,
но не увеличивает маленькие — отсюда крошечный повисший в углу
квадрат вместо растянутого на колонку. Убрал `.img-fluid`, обернул
`<img>` в структуру классов самой темы `.details-gallery-images >
.single-img` (`show.php`) — у неё уже есть готовое правило `width:
100%` в `style.css`, ровно то же, что растягивает эти же файлы в
мини-карточках каталога. Своего нового CSS не добавлял.

Проверено `curl`: разметка `.details-gallery-images > .single-img >
img#product-main-image` рендерится корректно. `composer test` —
63/63, `storage/logs/app.log` не пополнился.

Не проверено визуально (нет браузера в сессии) — само растягивание
картинки на колонку. Изображение при этом остаётся источником 270×303
(мылится при увеличении) — качество фото ограничено ассетами темы, не
код; настоящие фото товаров появятся в Фазе 4 (`FR-ADM-001`, загрузка
в `public/uploads/products/`).
**Что следующее:** Ручная проверка Таска 4 в браузере (включая эту
правку) → Таск 4 → ✅ Завершён, затем `task-init` для Таска 5.

---

**Дата:** 17.09.2026
**Что сделано:** Второй раунд фиксов по тому же ручному ревью
пользователя — новый скриншот с 4 отдельными замечаниями к карточке
товара.

1. **Хлебные крошки лепились к хедеру.** `applyContentOffset()`
   выставлял `padding-top` РОВНО равным высоте хедера — контент
   начинался впритык, без визуального зазора. Добавлен запас
   `CONTENT_OFFSET_GAP = 24px` поверх точной высоты. Правка в общей
   функции — затрагивает не только карточку товара, но и все страницы
   с `.page-content-offset` (по идее, к лучшему везде).
2. **«Характеристики» лепились к фото снизу.** Причина —
   `.section-padding-02` даёт только `padding-top`, без
   `padding-bottom`; я же добавил `pt-0` (обнуление верхнего отступа)
   на следующую секцию «по инерции», в результате между двумя блоками
   не было вообще никакого зазора. Убрал `pt-0` у секций
   «Характеристики»/«Похожие товары» — они снова используют свой
   штатный `padding-top: 100px` из темы.
3. **Количество растянуто на всю ширину, кнопка лепится к нему
   снизу.** Причина — переиспользовал тему-шный `.single-form` для
   поля количества: у него `input { width: 100% }`, рассчитано на
   поля логина/регистрации, для узкого числового поля не подходит.
   Заменено на свою `.product-variant-selector__quantity` (90px,
   `flex`-ряд с кнопкой «В корзину» через `gap`).
4. **Нет галереи под фото товара — не как в макете.** Изначально
   намеренно упростил до одного `<img>` (в сидах на сочетание
   Вариант+цвет ровно одно фото — карусель показывать было бы нечего),
   но пользователь прямо просил галерею как в макете. Решение:
   миниатюры под главным фото (`#product-thumbnails`,
   `.details-gallery-thumbs`, своя вёрстка без swiper) — по одной на
   каждый цвет ТЕКУЩЕГО Варианта, клик меняет и фото, и `color` в
   форме корзины. Заменяет прежние текстовые кнопки «Цвет:» — та же
   функция (выбор цвета), но в виде, который просил пользователь;
   текстовая индикация цвета осталась отдельной строкой («Цвет: …»)
   для читаемости.

Технический нюанс, из-за которого чуть не остался скрытый баг:
контейнер `#product-thumbnails` рендерился в разметке, ТОЛЬКО если у
ПЕРВОГО (самого дешёвого) Варианта было ≥ 2 цветов — но у большинства
товаров сидов именно у первого Варианта ровно один цвет (несколько
цветов есть только у отдельных Вариантов, например «Экокожа» у
`divan-verona»). Если бы контейнера не было в DOM с самого начала, JS
при переключении на такой Вариант не смог бы никуда подставить
миниатюры. Исправлено — контейнер рендерится всегда, скрывается
атрибутом `hidden`, JS показывает/наполняет по факту.

Проверено `curl` против реальной БД: секции без `pt-0` — те же классы
`section-padding-02` у обеих; `#product-thumbnails hidden` есть в
разметке `divan-verona` (первый Вариант «Велюр» — один цвет);
`.product-variant-selector__cart-row` с количеством и кнопкой в одном
контейнере. `composer test` — 63/63, `storage/logs/app.log` не
пополнился. `node --check app.js` — синтаксис ок.

Не проверено визуально (нет браузера в сессии): сам зазор под хедером,
реальное расположение количества/кнопки в ряд, клик по миниатюре и
смена фото/цвета на экране.
**Что следующее:** Ручная проверка Таска 4 в браузере (все 4 фикса
разом) → Таск 4 → ✅ Завершён, затем `task-init` для Таска 5.

---

**Дата:** 17.09.2026
**Что сделано:** Третий раунд правок по тому же ручному ревью — новый
скриншот с 3 замечаниями. На этот раз впервые в сессии проверил живьём
через настоящий браузер (Playwright/Chromium, установлен временно в
`scratchpad`, не в проект — `npm install playwright` +
`npx playwright install chromium`), а не только `curl`/структурный
разбор HTML. Это сразу изменило диагностику первого пункта.

1. **«Хлебные крошки не кликабельны, возможно перекрывает шапка» — не
   баг.** Playwright: `document.elementFromPoint()` в центре ссылки
   возвращает именно `<a href="/catalog/myagkaya-mebel">` (никто её не
   перекрывает), `page-content-offset` padding-top — 168px при высоте
   хедера 144px (запас `+24px` из прошлого фикса применяется
   корректно). Клик с таймаутом 3с не успевал — с таймаутом 15с
   успешно долетает до `/catalog/myagkaya-mebel`. Причина — не код, а
   реальная задержка сети до `mikhail700.beget.tech`: голое
   подключение PDO — ~1.5с, каждый последующий запрос — ~300-400мс;
   страница каталога делает несколько запросов подряд (счётчик,
   список товаров, `attachCheapestVariant`, `getFilterOptions` ×2,
   `getCategoryTree` для меню шапки) — итого 4-5с на весь ответ.
   Локально при реальном деплое на тот же shared-хостинг (PHP и MySQL
   на одном сервере, без сетевого перегона через интернет) эта
   задержка исчезнет сама по себе — это артефакт разработки с
   удалённой БД, не баг кода. Кода не менял.
2. **Текст срока изготовления лепился к количеству/кнопке.**
   `.product-variant-selector__lead-time` — `margin-bottom` 16px →
   24px.
3. **Кнопка «В корзину» растянута.** `flex: 1 1 auto` заставлял её
   занимать весь остаток строки рядом с узким 90px-полем количества.
   Заменено на `flex: 0 0 auto` + свой `padding: 0 32px` — кнопка
   снова по размеру содержимого, как в макете.

Заодно ответил на вопрос пользователя (галерея вариантов появится
после добавления реальных фото?) — да: миниатюры и главное фото уже
полностью управляются данными `variant_images.path` из БД, код никак
не завязан на конкретные файлы-заглушки; как только Фаза 4
(`FR-ADM-001`) заведёт реальную загрузку в `public/uploads/products/`
и сиды/данные будут ссылаться на настоящие фото, тот же механизм
покажет их без каких-либо изменений кода.

Проверено: полный скриншот страницы (`page.screenshot({fullPage:
true})`) визуально подтверждает все три фикса — крошки на своём
месте, срок изготовления не лепится, кнопка компактная. `composer
test` — 63/63, `storage/logs/app.log` не пополнился. Тестовые
`node_modules`/Playwright из scratchpad не попадают в репозиторий
(вне `c:\OSPanel\home\domform-eccomerce`).
**Что следующее:** Ручная проверка Таска 4 пользователем (задержка
БД — за пределами кода, остальное фактически подтверждено скриншотом
из Playwright) → Таск 4 → ✅ Завершён, затем `task-init` для Таска 5.

---

**Дата:** 17.09.2026
**Что сделано:** Четвёртый раунд правок по тому же ручному ревью —
скриншот показал подвал, прилипший прямо к блоку «Похожие товары».

Причина: «Характеристики» и «Похожие товары» были двумя отдельными
секциями `.section-padding-02` (только `padding-top`, без
`padding-bottom` — тот же класс, что уже чинил в прошлый раунд для
зазора МЕЖДУ ними, но забыл про зазор ПОСЛЕ последней). Объединил их в
одну секцию `.section-padding` (верх+низ) — таблица характеристик и
блок похожих товаров теперь просто два условных блока внутри одного
`<div class="container">`, с `mt-5` у заголовка «Похожие товары», если
над ним уже есть характеристики.

Заодно проверил на реальных сидах граничный случай: если бы остались
раздельные секции, при отсутствии «Похожих товаров» последней
рендерилась бы именно «Характеристики» — и с `.section-padding-02` она
так же лепилась бы к футеру. Такой товар реально существует:
«Шкаф-купе «Гардероб»» — единственный АКТИВНЫЙ товар в категории
«Шкафы» (второй, «Шкаф «Классик»», деактивирован ещё в Таске 1 для
проверки скрытия), `getRelatedProducts()` для него закономерно
возвращает пустой массив. Общее решение (одна секция на оба блока)
закрывает сразу оба случая, не только тот, что был на скриншоте.

Проверено Playwright (Chromium, тот же временный `scratchpad`-стенд):
- `divan-krovat-oslo` (обычный товар, есть похожие) — на скриншоте
  виден чистый отступ перед футером.
- `shkaf-kupe-garderob` (похожих товаров нет) — `getBoundingClientRect`
  подтверждает зазор 116px между низом таблицы характеристик и
  футером; заголовок «Характеристики» присутствует (`count() === 1`) —
  на первом скриншоте казался пропавшим, но это оказался чисто
  визуальный артефакт `position: fixed` баннера cookie при захвате
  full-page скриншота (баннер «плавает» поверх контента вместо реального
  прижатия к низу видимой области, свойство самого механизма
  full-page снятия скриншота у Chromium/Playwright для fixed-элементов,
  не баг вёрстки) — без баннера (клик по «Понятно» перед снимком) и
  через явную проверку `boundingBox()` заголовок на месте.

`composer test` — 63/63, `storage/logs/app.log` не пополнился.
**Что следующее:** Ручная проверка Таска 4 пользователем — на этот
момент все репортированные визуальные замечания (4 раунда) подтверждены
исправленными через Playwright-скриншоты и `boundingBox()`-замеры;
единственное, что нельзя перепроверить автоматически без реального
клика мышью — сама интерактивность (переключение Варианта/цвета,
блокировка «В корзину» без выбора). После проверки → Таск 4 →
✅ Завершён, затем `task-init` для Таска 5 (поиск с подсказками).

---

**Дата:** 17.09.2026
**Что сделано:** Пятый раунд правок по тому же ревью — пользователь
прислал скриншот верхней части карточки товара из оригинального
макета темы (баннер с именем страницы, картинка мебельной полки на
голубом фоне) и сам загрузил файл `public/uploads/baners/
page-banner-product.webp` (папка `baners` — так создал пользователь,
не переименовывал), попросив привести нашу карточку к оригиналу.

Декоративный баннер (`.page-banner-section`) был осознанно пропущен
ещё при планировании Таска 4 — в теме он держится на
`style="background-image: url(...)"` прямо в HTML, а `general.md`
прямо запрещает inline-стили. Раньше это означало «баннера не будет
вовсе» (тот же подход, что на `/catalog` и `/login`, которые тоже без
баннера). Теперь, когда картинка — реальный загруженный файл, это
решается штатно: фон через свой CSS-класс, а не через атрибут `style`.
Добавлено:
- `.page-banner-section.page-banner-section--product` в `show.php` —
  заголовок (имя товара, `<h2>`, чтобы не плодить второй `<h1>` —
  единственный `<h1>` страницы остаётся в `.product-details-
  description`, как и был) и хлебные крошки внутри (тот же компонент
  `breadcrumbs.php`, без изменений — тема стилизует `.breadcrumb li`
  что снаружи баннера, что внутри `.page-banner-content`, так что
  переносить компонент в новый контекст можно было без правок
  разметки).
- `.page-banner-section--product { background-image: url(...) }` в
  `app.css` — единственное новое правило.
- Убран `page-content-offset` с первой секции: с настоящим декоративным
  баннером хедер по дизайну темы должен прозрачно НАЛЕГАТЬ на его
  верхнюю часть (тот же приём, что уже на Главной со слайдером), а не
  отступать от него отдельным зазором — это сразу отдельная секция от
  ряда «фото + описание», у которой уже был свой `section-padding-02`
  без offset.

Проверено Playwright: `getComputedStyle(...).backgroundImage`
подтверждает реальный резолв в `/uploads/baners/page-banner-product.webp`
(200 OK); полный скриншот страницы визуально соответствует присланному
оригиналу (баннер с именем товара и крошками, хедер поверх, ниже —
фото/варианты/характеристики/похожие без регрессий по прошлым 4
раундам фиксов). `composer test` — 63/63, `storage/logs/app.log` не
пополнился.

Не проверено: страницы `/catalog` и `/login` по-прежнему без баннера
(вне скоупа этого запроса — пользователь просил именно карточку
товара); если баннер нужен и там, потребуется свой загруженный файл на
каждую страницу/тип страницы и отдельный запрос.
**Что следующее:** Ручная проверка Таска 4 пользователем — теперь все
5 раундов визуальных замечаний подтверждены Playwright-скриншотами.
После проверки → Таск 4 → ✅ Завершён, затем `task-init` для Таска 5.

---

**Дата:** 17.09.2026
**Что сделано:** Шестой раунд правок — пользователь прислал скриншот
блока «Information / Description / Reviews (03)» из оригинального
макета и попросил привести наш блок «Характеристики» к такому же виду
(вкладки), явно уточнив, что вкладку отзывов делать не нужно (Фаза 6).

`show.php` — «Характеристики» и «Описание» стали вкладками
`.product-details-tabs` (разметка и CSS-классы — точная копия
`product-details-affiliate.html`/`style.css`: `.nav.justify-content-
center`, `.tab-content`, `.tab-pane.fade`; переключение — нативный
Bootstrap `data-bs-toggle="tab"`/`data-bs-target`, свой JS не
понадобился вообще, тема уже всё анимирует через `::before` с
подсветкой активной вкладки). В таб «Характеристики»
(`.information-content`) подставлена уже существующая наша таблица
`product-specs-table` вместо текстовой заглушки темы («Fabric & care»/
«Size») — она и так была готова, просто раньше стояла отдельным блоком
вне вкладок.

Заодно убрал дублирующий абзац описания, который раньше показывался
СВЕРХУ (над селектором Варианта, рядом с ценой) — в оригинальной теме
там тоже есть текст, но ДРУГОЙ (иной lorem ipsum, чем в табе
Description) — у нас же только одно поле `products.description`,
показать его дважды на одной странице выглядело бы как баг
копипаста, а не как осознанное решение.

Логика показа вкладок: нав с переключателем (`<ul class="nav">`)
рендерится только если есть ОБА раздела — характеристики
(`product_specs`) И описание (`products.description`); если данных
только одного вида, показывается сразу этот раздел без вкладки-
переключателя (нечего переключать). Раньше уже был отдельный кейс —
«Шкаф-купе «Гардероб»» без похожих товаров, теперь до кучи
«Кресло-качалка «Уют»» без характеристик (только описание) — оба
кейса существуют на реальных сидах Таска 1, оба проверены.

Проверено Playwright (тот же `scratchpad`-стенд): `divan-krovat-oslo`
(характеристики + описание) — скриншот вкладок визуально идентичен
присланному оригиналу (оранжевая активная вкладка с подчёркиванием,
серая неактивная, разделительная линия снизу нава), клик по
«Описание» реально переключает контент (проверено кликом через
Playwright, не только вёрсткой) — Bootstrap data-api сработал без
моего JS. `kreslo-kachalka-uyut` (только описание, `product_specs`
пусто) — нав не рендерится, текст описания сразу виден, по центру
(тема: `.description-content { text-align: center }`). По ходу нашёл
и поправил баг в собственном тестовом скрипте (не в проекте) — второй
клик по уже скрытому cookie-баннеру (localStorage запомнил первое
согласие) вешал Playwright на таймаут; добавил проверку `isVisible()`
перед кликом.

`composer test` — 63/63, `storage/logs/app.log` не пополнился.
**Что следующее:** Ручная проверка Таска 4 пользователем — 6 раундов
визуальных замечаний подтверждены Playwright. После проверки → Таск 4
→ ✅ Завершён, затем `task-init` для Таска 5 (поиск с подсказками).

---

**Дата:** 17.09.2026
**Что сделано:** Седьмой, самый маленький раунд правок — скриншот
показал заголовок «Похожие товары», слипшийся с таблицей
характеристик над ним (стрелкой указано место склейки).

Причина: заголовок использовал Bootstrap-утилиту `.mt-5`, но эта тема
переопределяет весь набор `.mt-*` под свой, более узкий масштаб
отступов — `.mt-5 { margin-top: 1.625rem !important; }`, то есть 26px,
а не привычные Bootstrap-овские 48px (3rem). Рядом с плотной таблицей
характеристик (мелкий шрифт, тонкие строки) 26px читались как «почти
нет отступа». Заменил класс `mt-5` на свой `.product-section-gap`
(`margin-top: 48px`) в `app.css` — обычный кастомный класс, не завязан
на чужую систему масштабов темы.

Проверено Playwright: `getComputedStyle(heading).marginTop` — было
`26px`, стало `48px`; замер `boundingBox()` подтверждает физический
зазор 48px между низом блока вкладок и верхом заголовка; скриншот
показывает отчётливое разделение. `composer test` — 63/63,
`storage/logs/app.log` не пополнился.
**Что следующее:** Ручная проверка Таска 4 пользователем — 7 раундов
визуальных замечаний подтверждены Playwright. После проверки → Таск 4
→ ✅ Завершён, затем `task-init` для Таска 5 (поиск с подсказками).

---

**Дата:** 17.09.2026
**Что сделано:** Восьмой раунд правок — пользователь попросил выровнять
текст в блоке «Описание»/«Характеристики» по левому краю вместо
центра. Таблица характеристик (`.product-specs-table`) и так была по
левому краю (ячейки `<th>`/`<td>` не центрируются по умолчанию) —
реальная правка касалась только вкладки «Описание»: тема задаёт
`.description-content { text-align: center; margin-top: 35px; }`.
Добавлено переопределение `.description-content { text-align: left; }`
в `app.css` (после `style.css` в порядке подключения — побеждает по
каскаду без `!important`).

Проверено Playwright: `getComputedStyle(...).textAlign` на вкладке
«Описание» после клика — `left` (было бы `center` без правки);
скриншот подтверждает визуально — абзац теперь начинается от левого
края области вкладки, как и таблица характеристик на соседней
вкладке. `composer test` — 63/63, `storage/logs/app.log` не
пополнился.
**Что следующее:** Ручная проверка Таска 4 пользователем — 8 раундов
визуальных замечаний подтверждены Playwright. После проверки → Таск 4
→ ✅ Завершён, затем `task-init` для Таска 5 (поиск с подсказками).

---

**Дата:** 17.09.2026
**Что сделано:** Таск 5 Фазы 1 (`task-init` → реализация): поиск по
каталогу. `src/Core/CatalogFilters.php` — `normalizeSearchQuery()`
(trim/схлопывание пробелов/обрезка длины) и `buildFulltextTerm()`
(вырезает операторы BOOLEAN MODE `+ - * " ( ) < > ~ @` из каждого
слова, добавляет `*`); 7 новых тестов. `src/Models/Product.php` —
`searchProducts()`/`countSearchProducts()`/`suggestProducts()` с общим
`buildSearchConditions()`: `MATCH...AGAINST` по `products(name,
description)` от 3 символов, иначе префиксный `LIKE 'q%'` по
`products.name`; материал/цвет — `LIKE 'q%'` по
`product_variants.material`/`variant_images.color` в обоих случаях.
`SearchController` (`index()`, `suggest()` — JSON,
`tooManyAttempts('suggest', 20, 10)`). Формы поиска в `header.php`
(desktop + мобильная) переведены на `GET /search`; подсказки —
`.search-suggest` рядом с каждым полем, JS (`initSearchSuggest()`) —
debounce 250мс, ↑↓/Enter/Esc, закрытие по клику вне.

Два отклонения от изначального `TASK.md` (записаны в нём же, раздел
«Отклонения от плана»): (1) `src/Views/search/index.php` создан
отдельно, не переиспользован `catalog/index.php`/`catalog-grid.php`
буквально — их `<select id="catalog-sort">` жёстко привязан к
`form="catalog-filter-form"`, которого на `/search` нет и не должно
быть (сайдбар каталога вне скоупа), и весь JS каталога (сортировка,
пагинация через fetch) молча выключается без этой формы; новая
страница переиспользует только действительно общие компоненты
(`product-card.php`/`pagination.php`/`empty-state.php`), сортировка —
маленькая форма с автосабмитом, пагинация — обычные ссылки, без fetch
(в `phase-1.md` для `/search`, в отличие от каталога Таска 3,
требования «без перезагрузки» нет). (2) Добавлен `INDEX(material)` на
`product_variants` (`database/install.php`, `database.md`, `ADR-032`)
— новый `LIKE`-фильтр по материалу, тот же принцип, что `INDEX(color)`
в `ADR-031` (Таск 3).

Проверено `curl` против реальной БД (`mikhail700.beget.tech`, после
`php database/install.php` дважды подряд — без ошибок): «диван» → 4
товара (FULLTEXT), «ди» → 3 (префиксный LIKE по имени), «экокожа» → 3
(материал), «Коричневый» → 2 (цвет), несуществующий запрос → пустое
состояние, `q` сохраняется в скрытом поле при смене сортировки,
`+*"` не роняет ни `/search`, ни `/search/suggest` (оба 200),
`/search/suggest` отдаёт `Content-Type: application/json`. Rate-limit
подтверждён изолированным вызовом `tooManyAttempts()`/`hitRateLimit()`
(1–20 `ok`, 21–25 `BLOCKED`) — HTTP-бёрст через `php -S` не показал
это надёжно: дев-сервер спавнит процесс на каждый запрос, 25
последовательных `curl` не уложились в 10-секундное окно, оно успевало
«истечь» раньше порога. Попутно найдено (не в скоупе, не трогал):
`hitRateLimit()` не сбрасывает `first_at` при истечении окна — после
первого «протухшего» окна блокировка для этого действия+IP перестаёт
срабатывать, пока файл не удалят (`clearRateLimit()`); тот же код уже
используется `login`/чекаутом. `node --check app.js` — без ошибок.
`composer test` — 70/70, `storage/logs/app.log` не пополнился новыми
записями (те же 2 старые строки, что в Таске 4).
**Что следующее:** Ручная проверка Таска 5 пользователем (клик по
подсказкам, реальная навигация клавиатурой — нет браузера в сессии).
После подтверждения → Таск 5 → ✅ Завершён (уже отмечено в `phase-1.md`
по факту `curl`-проверки), далее — закрытие Фазы 1 (`_status.md`,
`tz-coverage.md`) и переход к Фазе 2.

---

**Дата:** 17.09.2026
**Что сделано:** Закрытие Фазы 1 по чек-листу «Закрытие фазы»
(`phase-1.md`): `.docs/phases/_status.md` — Фаза 1 → ✅ Завершена (и
дублирующий заголовок статуса в самом `phase-1.md`, тот же факт в двух
местах одного документа); `.docs/tz-coverage.md` — в таблицу «Модуль/ID
→ Фаза» добавлены заметки о реализации (по образцу уже закрытой Фазы
0, `FR-AUTH-001…005`): `FR-CAT-001…008` — Тасками 2–3, `FR-CARD-001…005,
007…009` — Таском 4, `FR-SRCH-001…004` — Таском 5, `BR-004`
(отображение) — Тасками 2–4. Сама разбивка диапазонов по фазам не
менялась — она зафиксирована на этапе `sdd-roadmap` (`ADR-017`) и
подтвердилась без расхождений по факту реализации.
**Что следующее:** Фаза 2 — корзина и оформление заказа (`task-init`
для Таска 1 Фазы 2, `.docs/phases/phase-2.md`, если ещё не создан —
иначе `sdd-roadmap`/`phase-init` для Фазы 2 сначала).

---
**Дата:** 18.09.2026
**Что сделано:** Багфикс вне фазы (каталог, Таск 3 Фазы 1) — по
сообщению пользователя «при использовании фильтра страница постоянно
в загрузке, фильтры зацикливаются». Причина: `ion.rangeSlider` сам
триггерит `change` на своём `<input id="catalog-price-slider">` при
каждом сдвиге ползунка и при `update()`; input лежит внутри
`#catalog-filter-form`, и делегированный обработчик `change` в
`app.js` вызывал `applyCatalogFilters()` → `fetch` + `pushState` на
каждый mousemove (плюс ещё один из `onFinish`). Десятки параллельных
запросов к удалённой БД, ответы вразнобой — визуально «вечная
загрузка». Второй дефект того же корня: «Сбросить фильтры» вызывал
`update()` до очистки hidden `price_min/max` → лишний fetch со
старыми значениями гонялся со сбросом. Правки только в
`public/assets/js/app.js`: (1) слайдер исключён из селектора `change`
(`input:not(#catalog-price-slider)`) — цену применяет только
`onFinish`; (2) в `resetCatalogFilterForm()` hidden-поля очищаются до
`update()`; (3) `loadCatalogResults()` держит один запрос в полёте
через `AbortController` — новый фильтр отменяет предыдущий,
`AbortError` не считается сбоем сети (без этого `catch` делал полную
перезагрузку страницы). `node --check` — без ошибок. Не в скоупе, не
трогал: в `app.log` серия `MySQL server has gone away` /
`getaddrinfo failed` к `mikhail700.beget.tech` 17.09 — нестабильное
соединение с удалённой БД усиливает симптом, но не является причиной.
**Что следующее:** ручная проверка в браузере (перетаскивание
ползунка цены → один запрос по отпусканию; «Сбросить фильтры» → одна
выдача без фильтров; вкладка Network — нет стопки запросов), затем
Фаза 2 (`_status.md` → 🔄, `task-init` Таска 1).

---

**Дата:** 18.09.2026
**Что сделано:** Таск 1 Фазы 2 — схема под корзину, Model корзины,
расчёты (без UI). `cart_items.price_snapshot DECIMAL(10,2) NOT NULL`
(`ADR-033`); `session_id` с этого таска хранит cookie `cart_token`, не
PHP session id (`ADR-034`, `functions.php`: `cartToken()`/
`cartOwner()`). Чистые расчёты в `Core/Cart.php` (`clampCartQuantity()`,
`calculateCartTotals()` — суммы через bcmath, без float,
`findPriceChanges()` — через `bccomp`, устойчиво к разнице
форматирования `"1999.9"`/`"1999.90"`); `Models/Cart.php` —
`getCartItems()`/`addCartItem()`/`updateCartItemQuantity()`/
`removeCartItem()`/`countCartItems()`/`clearCart()`/`mergeGuestCart()`,
все с проверкой владельца через общий `validateCartOwner()` (бросает
исключение при пустом/двойном ключе). Проверено против реальной БД
(`mikhail700.beget.tech`, с подтверждения пользователя): `install.php`
дважды подряд без ошибок, структура сверена; временным скриптом в
scratchpad — изоляция владельца, слияние Вариант+цвет, clamp образца,
`mergeGuestCart()` (суммирование, идемпотентность повторного вызова),
недоступность чужой строки для update/remove. `composer test` 84/84
(+14 `CartTest`).
Отклонение от плана `TASK.md`: `CART_MAX_QUANTITY` определена в
`Core/Cart.php`, а не в `config/config.php` — `tests/bootstrap.php` не
подключает `config.php` (не идемпотентен по `ROOT_PATH`, требует
`.env`), тот же принцип, что уже применялся к
`SEARCH_QUERY_MAX_LENGTH` в `CatalogFilters.php`.
**Что следующее:** Таск 2 Фазы 2 — добавление в корзину, страница
корзины, счётчик в шапке (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Таск 2 Фазы 2 — добавление в корзину, страница
корзины, счётчик в шапке. `CartController` (`index`/`add`/`update`/
`remove`, `requireCsrf()` на всех POST); `redirectBack()` после `add()`
берёт только путь из `Referer` (без хоста) — защита от open redirect.
`cart/index.php` + `components/cart-row.php` по макету
`cart.html`/`empty-cart.html`, без «Calculate Shipping»/«Coupon Code»;
недоступная позиция (активный `reserve`, данные уже были в
`getCartItems()` с Таска 1) помечена и исключена из суммы —
`.cart-row--unavailable`. Иконка корзины в шапке → `/cart`,
`$cartCount` считается прямо в `header.php` (не через `render()`, см.
отклонение ниже). `mergeGuestCart()` подключена в `AuthController::
login()`/`register()` и `attemptRememberLogin()`. `app.js`:
`initCartQuantityControls()` — автосабмит формы по клику `.add`/`.sub`
(значение `<input>` меняет тема-шный `main.js`, наш обработчик ловит
клик во всплытии на `document` и сабмитит форму).
Проверено `php -S` + `curl` против реальной БД: добавление с цветом
(нашёл и исправил артефакт теста — `curl`/bash на Windows передавали
кириллицу в CP1251 вместо UTF-8 при инлайне в команде; сама
`addCartItem()` прямым вызовом уже работала верно, дальше тестировал
через `--data-binary` с заранее percent-encoded UTF-8), слияние
Вариант+цвет, образец (disabled «+», `quantity=1`), пересчёт суммы,
изоляция владельца (чужой `item_id` не меняется), недоступная позиция
(0 ₽ в сумме), слияние гостевой корзины при `/login` — в т.ч. из
«другого браузера» (новый cookie-jar, тот же пользователь). Все
тестовые данные (заказ/резерв/пользователь/строки корзины) удалены
после проверки, `app.log` не пополнился. `composer test` 84/84 (новых
тестов нет — таск целиком UI/DB-поток).
Отклонение от плана `TASK.md`: `$cartCount` вычисляется прямо в
`header.php` (`countCartItems(cartOwner())`), а не через `render()` —
`header.php` уже сам вычисляет всё нужное (`isAuthenticated()`,
`getCategoryTree()`, `normalizeSearchQuery()`), добавление
`$cartCount` в `render()` было бы единственным исключением из этого
паттерна.
**Что следующее:** Таск 3 Фазы 2 — экран оформления: форма, пересчёт
цен, недоступный образец (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Багфикс вне фазы (корзина, Таск 2 Фазы 2) — по
сообщению пользователя «добавление в корзину и переход по страницам
стали долгими». Причина: `header.php` (подключается на каждой
странице сайта) с Таска 2 на каждый рендер выполнял
`countCartItems(cartOwner())` — отдельный запрос к БД. Замер показал
`mikhail700.beget.tech` отвечает на тривиальный запрос ~300мс (сеть до
shared-хостинга), а `connect()` — ~1.5с; лишний запрос на каждой
странице ощутимо добавлялся поверх и без того тяжёлой БД. Фикс:
счётчик кэшируется в `$_SESSION['cart_count']`
(`currentCartCount()`/`refreshCartCount()` в `functions.php`) — читается
из сессии без похода в БД, пересчитывается только там, где корзина
реально меняется (`CartController::add/update/remove/clear`,
`AuthController::login/register`, `attemptRememberLogin()` — после
`mergeGuestCart()`, т.к. смена владельца делает старое кэшированное
число невалидным), инвалидируется на `logout()`. Замер: `/catalog`
подряд без кэша ~4.5с, с прогретым кэшем ~3.7с — экономия одного
сетевого круга подтверждена; сама базовая задержка БД (~300мс/запрос,
~1.5с на коннект) — не код-баг, инфраструктурное свойство удалённого
shared-хостинга, вне скоупа фикса.
Заодно (тот же скриншот пользователя — эталон вёрстки из темы,
`00-input/design/cart.html`) — сверил `/cart` вживую в браузере
(`Playwright`, впервые в этом проекте: `chromium-cli` недоступен,
поднял `playwright`-пакет в scratchpad поверх уже закэшированного
Chromium). Первый скриншот выглядел полностью без CSS — оказалось,
артефакт моего же `php -S ... public/index.php`: без роутер-скрипта,
имитирующего `public/.htaccess` (`RewriteCond %{REQUEST_FILENAME} !-f`),
статика уходила в `index.php` и получала 404; написал временный
`dev-router.php` в scratchpad (не в репозитории). Второй артефакт —
на полностраничном скриншоте пропадала шапка таблицы (`<thead>`) под
cookie-баннером — оказалось «привидение» полностраничных скриншотов
Playwright: `position: fixed` элемент (cookie-уведомление) впечатывается
в каждый склеенный кусок кадра. Скриншот одного элемента (`.cart-wrapper`)
без full-page подтвердил: вёрстка уже соответствует эталону
1:1 (серый заголовок таблицы, тёмная пилюля `−1+`, суммы, иконка
удаления) — не хватало только кнопок «Очистить корзину»/«Обновить
корзину» из демо. Добавил «Очистить корзину» —
`CartController::clear()` (использует готовый `clearCart()` из Таска 1)
+ `POST /cart/clear`; «Обновить корзину» сознательно не добавлена —
избыточна при уже реализованном автосабмите по клику ±, кнопка без
действия была бы мёртвым UI.
`composer test` 84/84; все тестовые данные (корзины, временный
пользователь) удалены, `app.log` не пополнился.
**Что следующее:** Таск 3 Фазы 2 — экран оформления (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Ещё два багфикса вне фазы (корзина) по сообщению
пользователя.
1. **Счётчик в шапке расходился с содержимым `/cart` (показывал 2,
   страница пустая).** Причина — моя же предыдущая проверка: кэш
   `$_SESSION['cart_count']` (сегодняшний фикс производительности)
   переживает мутации `cart_items` в обход приложения, а я несколько
   раз чистил таблицу напрямую SQL между проверками на **той же общей
   удалённой БД**, где пользователь параллельно тестировал через
   браузер — его сессия держала кэш от товаров, которые я потом стёр
   напрямую. Воспроизвёл точно: add → кэш 2 → `DELETE FROM cart_items`
   в обход приложения → счётчик остаётся 2, `/cart` пустая. Фикс —
   самоисцеление: `CartController::index()` теперь кладёт в кэш
   (`cacheCartCount()`, новая функция в `functions.php`) сумму
   количеств из уже прочитанных `$items` без лишнего запроса — открыв
   `/cart`, счётчик в шапке гарантированно совпадёт с тем, что на
   странице, чем бы кэш ни разошёлся.
2. **Иконка удаления в корзине — с рамкой/фоном кнопки браузера, без
   оранжевого hover, как в оригинале темы.** Причина: тема красит
   `.product-action a` (`style.css`: `a{color:#f2a100}`,
   `a:hover{color:#c28100}`), а у нас там `<button class="remove">`
   (не `<a href>` — CSRF-форма, `general.md`: «POST всегда
   заканчивается redirect()», executable GET-ссылка на мутацию
   отклонена как первый инстинкт) — правила на `a` на `button` не
   действуют, плюс браузер добавляет свою рамку/фон кнопке по
   умолчанию. Добавлен явный сброс + те же цвета в `app.css`
   (`button.remove` / `button.remove:hover`).
Оба фикса проверены вживую в браузере (Playwright): (1) добавление →
внешний `DELETE` → открытие `/cart` → счётчик самоисцелился до 0 везде
дальше по сайту; (2) скриншот кнопки в обычном состоянии и на hover —
без рамки, оранжевая, темнее на hover, идентично эталону.
`composer test` 84/84, тестовые данные удалены, `app.log` не
пополнился.
**Что следующее:** Таск 3 Фазы 2 — экран оформления (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Багфикс вне фазы (каталог, Таск 2 Фазы 1) — по
скриншоту пользователя: футер прилипает вплотную к карточкам товаров
на `/catalog`. Причина: единственная секция с товарами обёрнута в
`section-padding-02` (только верхний отступ 100px) — по образцу темы
этот класс годится лишь когда следом идёт ещё одна секция, чей
верхний паддинг компенсирует стык (так и сделано на `/product/{slug}`:
`section-padding-02` для галереи/вкладок, затем `section-padding` — с
нижним отступом — для «Похожие товары», последней секции перед
футером). На `/catalog` эта вторая секция никогда не заводилась —
нижнего отступа не было вообще. Фикс: `section-padding-02` →
`section-padding` в `src/Views/catalog/index.php`. Остальные страницы
(`home.php`, `search/index.php`, auth-формы, `admin/index.php`)
сверены — там уже везде `section-padding` или корректная пара, тот же
баг больше нигде не повторяется. Проверено в браузере (Playwright):
зазор перед футером — 100px, совпадает с остальными страницами.
`composer test` 84/84.
**Что следующее:** Таск 3 Фазы 2 — экран оформления (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Замерил причину «долгих переходов» по шагам
(`/catalog`, реальная БД `mikhail700.beget.tech`): подключение — 1944мс,
6 запросов по ~300-650мс каждый, итого ~4.1с. Сырой TCP-коннект до
хоста — 4мс (сеть в порядке), а первый байт от самого MySQL-сервера
после коннекта — 1.37с: тормозит не канал, а сам сервер БД на
shared-хостинге (перегружен/троттлит подключения). Решение
пользователя: запросы в `Product.php` не объединять (заявленный план —
после переезда на боевой хостинг сеть станет быстрее, трогать
протестированный код Фазы 1 незачем), `PDO::ATTR_PERSISTENT` не
включать. Вместо этого — индикатор загрузки при переходах между
страницами: `src/Views/components/page-loader.php` (оверлей +
спиннер, подключён в `footer.php`), `app.js:initPageLoader()` —
показывается по нативному `beforeunload`, а не по перехвату клика/
сабмита: AJAX-переходы каталога (`fetch()`+`preventDefault()`, без
настоящей навигации) никогда не порождают `beforeunload`, поэтому
лоадер физически не может показаться на них по ошибке — не пришлось
заводить и поддерживать список исключений. Без `preventDefault()`/
`returnValue` в обработчике — иначе браузер показал бы системный
диалог подтверждения ухода со страницы. Проверено в браузере
(Playwright): скрыт по умолчанию, не активируется на AJAX-фильтре
каталога, активируется по `beforeunload` (класс `is-active`,
скриншот подтверждает оверлей + спиннер поверх контента). `composer
test` 84/84.
**Что следующее:** Таск 3 Фазы 2 — экран оформления (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Таск 3 Фазы 2 реализован и проверен `curl`-ом против
реальной БД (`mikhail700.beget.tech`, тестовые данные удалены после
проверки): `GET /checkout` (пустая корзина → редирект в `/cart`;
предзаполнение из аккаунта для Покупателя; уведомление «было/стало» +
блокировка «Оформить» до `POST /checkout/accept-changes`; активный
`reserve` на образец → позиция недоступна, исключена из итога, удаляется
из корзины после принятия) и `POST /checkout/accept-changes`. Отклонение
от плана: `Models/Cart.php` пришлось тронуть (`acceptCartPriceChanges()`)
— UPDATE price_snapshot/DELETE недоступных это SQL, ему не место в
контроллере (`php.md`); `tests/bootstrap.php` — добавлен `require_once
Core/Checkout.php`. `<form id="checkout-form">` обёрнут только вокруг
левой колонки — правая (`checkout-summary.php`) содержит свою форму
«Принять изменения», вложенные `<form>` невалидны в HTML; кнопка
«Оформить заказ» связана через HTML5 `form="checkout-form"`. Раскрытие
Email/Пароль по чекбоксу «Создать аккаунт?» переиспользует готовый
переключатель темы (`main.js`: `#account` → `.checkout-account`) —
собственный JS не понадобился. `SHOP_PHONE`/`SHOP_WHATSAPP_URL` —
плейсхолдер-заглушка, реального номера магазина нет ни в `.env`, ни в
`prd.md` (заменить перед продакшеном). `composer test` 98/98 (+14
`CheckoutValidationTest`). Визуальная проверка вёрстки на 320px+ в
браузере не проводилась (нет браузера в среде) — только структурная
проверка `curl`-ом.

Вне таска, по репорту пользователя со скриншотом: на `/cart` кнопка
«Очистить корзину» обрезалась на ≤575px — тот же баг темы, что уже
чинили у кнопок логина/регистрации (`.btn{overflow:hidden}` +
фиксированная `width:48%`, рассчитанная под короткий английский текст);
исправлено в `app.css`. Также по предложению пользователя — таблица
корзины на <992px (Bootstrap `lg`) заменена на список карточек чисто
CSS-приёмом («таблица → список»: `<thead>` скрыт, `<tr>` становится
карточкой, `data-label` на `<td>` в `cart-row.php` подписывает
Количество/Сумму) вместо горизонтальной прокрутки — без дублирования
разметки.
**Что следующее:** Таск 4 Фазы 2 — создание Заказа (`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Таск 4 Фазы 2 реализован и проверен `curl`-ом против
реальной БД (`mikhail700.beget.tech`, тестовые данные удалены после
проверки): `POST /checkout` создаёт Заказ (гость/Покупатель,
самовывоз/доставка, все 3 способа оплаты) со снэпшотом `order_items` и
суммой, пересчитанной из `product_variants` внутри транзакции;
`GET /checkout/success`. `Models/Order.php` — `createOrder()` (`SELECT
... FOR UPDATE` по Варианту, повторная проверка резерва на образец,
«ровно один источник контактов»), `linkGuestOrdersToUser()` (очищает
`guest_*` при привязке — вызывается из `AuthController::register()`).

Находки перед кодом (обе — пропуски прошлых тасков, не этого): в схеме
не было `orders.comment` для обязательного поля «Комментарий» из ТЗ
(`FR-CHK-001`) — добавлена `orders.comment TEXT NOT NULL` (`ADR-035`,
идемпотентный `ALTER` для уже развёрнутых БД, install.php дважды подряд
без ошибок); в форме `/checkout` (Таск 3) не было скрытого поля
`checkout_token`, хотя контроллер его генерировал — добавлено.

Отклонения от плана: (1) `Core/Checkout.php` не подключал
`Core/Validation.php`, хотя вызывает `normalizePhone()`/`validatePhone()`
и др. — баг молчал весь Таск 3, потому что `GET /checkout` не вызывает
`normalizeCheckoutInput()`; первый же ручной тест `POST /checkout` упал
(`Call to undefined function normalizePhone()`), исправлено
`require_once` в `Core/Checkout.php`. (2) `CheckoutController::index()`
отрефакторен в общий приватный `renderCheckoutPage()` — `store()` при
ошибках валидации должен показать ту же форму с `old`/`errors`, без
общего метода дублировал бы `index()`. (3) Ручная проверка отката
транзакции (DoD: «сбой на вставке order_items») — БД на
`mikhail700.beget.tech` работает с пустым `sql_mode` (не
`STRICT_TRANS_TABLES`): переполнение VARCHAR и нечисловые значения в
INT тихо коэрсятся вместо ошибки, порча данными не падает; проверено
через FK-нарушение (несуществующий `product_variant_id`) во временном
скрипте вне репозитория, повторяющем структуру транзакции
`createOrder()` — `orders`-строка создаётся, `order_items` падает,
`rollBack()` — `orders` пуст после отката.

Двойная отправка (`checkout_token`) и rate-limit (`tooManyAttempts`,
6-я попытка за минуту заблокирована, 5 предыдущих без блокировки)
проверены сериями быстрых `curl`-запросов. `composer test` 98/98 (без
новых unit-тестов — весь новый код ходит в БД, `php.md`: тесты на
DB-Model — отдельная категория, инфраструктура для неё уже есть).
Визуальная проверка `/checkout/success` на 320px+ в браузере не
проводилась (нет браузера в среде).
**Что следующее:** Таск 5 Фазы 2 — статусная модель Заказа и отмена
(`task-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Таск 5 Фазы 2 реализован и проверен. `Core/OrderStatus.php`
— константы 7 статусов Заказа + 3 статусов оплаты с русскими подписями
(`orderStatusLabel()`), `allowedOrderTransitions()`/`canTransitionOrder()`
по таблице раздела 6.3 ТЗ (образец пропускает «В производстве», ветвление
из «Готов к отгрузке» по способу получения), `canCancelOrder()`.
`Models/Order.php` — `orderHasShowroomSample()`, `transitionOrderStatus()`
(единственное место с `UPDATE orders SET status` во всём проекте —
подтверждено `grep`), `cancelOrder()`. `Views/checkout/success.php` —
статус через `orderStatusLabel()` вместо литерала. `tests/Unit/
OrderStatusTest.php` — 21 тест, вся таблица переходов включая
запрещённые. `composer test` 119/119.

Ручная проверка временным скриптом в scratchpad (не в репозитории)
против реальной БД (`mikhail700.beget.tech`): `cancelOrder()` на
`new`→`cancelled` с обновлением `updated_at`; на `delivered`→`false` без
изменений; цепочка `new→confirmed→in_production→ready_for_shipment→
shipping→delivered` проходит и заполняет `delivered_at`; образец
`confirmed→ready_for_shipment` напрямую, минуя `in_production`;
запрещённый `new→shipping` отклонён. `/checkout/success` проверен через
`curl` на реальном заказе — показывает «Новый» из `orderStatusLabel()`.

При уборке тестовых данных чуть не удалил чужой реальный заказ:
скрипт очистки убирал «самый свежий» заказ по `id` без предварительной
проверки и наткнулся на заказ №8 (имя «Михаил», реальные телефон/email,
два разных товара) — по счастливой случайности это оказался не самый
свежий на тот момент, поэтому не пострадал автоматически, но сам
подход был неправильным (нужно было сверяться с содержимым, а не
полагаться на порядок). Пользователь подтвердил, что заказ №8 —
тестовый, удалён по прямому запросу.

Закрытие Фазы 2 (`phase-2.md`, «Закрытие фазы»): `_status.md` — Фаза 2
→ ✅ Завершена, диапазон `FR-ORD-*` в строке фазы уточнён (`001,002,005`
вместо `001…003`); `tz-coverage.md` — `FR-ORD-001…005` разнесены по
фактическому покрытию (001/005 — Фаза 2, 002 — механизм Фаза 2 / UI
Фаза 4, 003/004 — целиком Фаза 4, ранее ошибочно объединены с 001/005
как «Фаза 2» и «Фаза 4» без разбивки на механизм/UI); `FR-CART-*`/
`FR-CHK-*`/`FR-SHIP-*`/`BR-001`/`BR-006`/`BR-007` — отметки
«Реализовано Тасками N Фазы 2».

Вне скоупа фазы, не чинилось (зафиксировано в `phase-2.md`): баг
`hitRateLimit()` (не сбрасывает `first_at` по истечении окна,
затрагивает `checkout`) — отдельный таск; email Менеджеру о новом
Заказе — ТЗ не требует.
**Что следующее:** Фаза 3 — онлайн-оплата и касса (`phase-init`).

---

**Дата:** 18.09.2026
**Что сделано:** Спланирована и создана `phase-3.md` (`phase-init`):
2 таска — заглушка оплаты картой и механизм фиксации оплаты без UI;
`FR-PAY-002` разрезан на механизм (Фаза 3) и UI Менеджера (Фаза 4) по
аналогии с `FR-ORD-002/003` из Фазы 2 (решение подтверждено
пользователем перед созданием файла). `TASK.md` записан и реализован
для Таска 1: `GET /payment/stub` (`PaymentController::stub()`) резолвит
Заказ через `$_SESSION['last_order_id']`, без id в URL — тот же приём,
что `/checkout/success`; редирект на `/` без сессии, редирект на
`/checkout/success` для способов оплаты кроме `card_online`;
`checkout/success.php` — текст про оплату картой заменён ссылкой на
`/payment/stub`. Проверено через `php -S` + `curl` против реальной БД
(`mikhail700.beget.tech`): заказ картой → ссылка и заглушка работают;
заказ наличными → ссылки нет, прямой заход редиректит; без сессии →
редирект на `/`; `composer test` 119/119; новых записей в
`storage/logs/app.log` нет. Тестовые заказы (№15, №16) удалены после
проверки.
**Что следующее:** Фаза 3, Таск 2 — механизм фиксации оплаты
(`markOrderPrepaid()`/`markOrderPaidFull()`, без UI) — `task-init`.

---

**Дата:** 18.09.2026
**Что сделано:** По результатам ручной проверки Таска 1 Фазы 3
пользователем в браузере найдены и исправлены 2 визуальных бага
`/payment/stub` и `/checkout/success`, не пойманных `curl`-проверкой
(HTML валиден, но не значит, что вёрстка выглядит нормально):
1) Кнопки «Позвонить»/WhatsApp на `/payment/stub` прилипали к серому
   информационному блоку сверху — причина: класс `mt-30`, скопированный
   с `checkout/success.php`, не существует ни в одном CSS-файле
   проекта (проверено `grep` по `style.css`/`app.css`) — ни на что не
   влиял. У `.checkout-info` отступ уже задан самой темой, а у
   `.checkout-call` в теме есть только `margin-bottom`, без
   `margin-top` — компонент рассчитан на то, что он первый элемент в
   `.container` (как на `/checkout`), а не идёт после другого блока.
   Убраны оба бесполезных `mt-30`, добавлено точечное правило
   `.checkout-info + .checkout-call { margin-top: 30px; }` в
   `public/assets/css/app.css` (по образцу других точечных фиксов
   темы в этом файле).
2) Ссылка «перейти к оплате» в тексте `/checkout/success` стилизована
   темой как обычный текст (`.checkout-info .info-header a { color:
   #000 }`, без подчёркивания, цвет меняется только на `:hover`) —
   неотличима от текста абзаца. Заменена на отдельную кнопку «Перейти
   к оплате» (`btn btn-dark btn-hover-primary`) под информационным
   блоком — тот же паттерн, что «Вернуться к заказу»/«Продолжить
   покупки».
Оба фикса проверены через `php -S` + `curl` (разметка/CSS отдаются
верно) и подтверждены пользователем в браузере — Таск 1 полностью
готов. `composer test` 119/119 после правок. Тестовые заказы удалены
после каждой проверки.
**Что следующее:** Фаза 3, Таск 2 — механизм фиксации оплаты
(`markOrderPrepaid()`/`markOrderPaidFull()`, без UI) — `task-init`.

---
