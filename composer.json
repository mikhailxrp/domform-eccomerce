{
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "seed:catalog": "php database/seed-catalog.php"
    },
    "config": {
        "sort-packages": true
    }
}
