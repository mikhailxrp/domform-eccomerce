# Current Task

## Фаза
Phase 1 — Каталог и карточка товара (`.docs/phases/phase-1.md`, Таск 4)

## Задача
`/product/{slug}` по макету `SCR-03` без «Buy Now»: галерея, выбор
материала/механизма (Вариант) и цвета, цена и срок изготовления из
Варианта, «Выставочный образец» вместо срока, таблица характеристик,
текст о доставке и о предзаказе, похожие товары той же категории,
кнопка «В корзину» с клиентской блокировкой без выбранного Варианта.

## Scope — что трогаем

- [x] `src/Models/Product.php` — изменить: `findProductBySlug()`
      (сразу с primary-категорией — нужна и крошкам, и «Похожим
      товарам»; неактивный товар не находится вовсе, тот же результат,
      что несуществующий slug), `getProductVariants(int $productId)`,
      `getVariantImages(array $variantIds)`, `getProductSpecs(int
      $productId)`, `getRelatedProducts(int $productId, int
      $categoryId, int $limit)` (переиспользует
      `attachCheapestVariant()` — тот же формат строки, что и листинг
      каталога, значит и тот же `product-card.php`)
- [x] `src/Controllers/ProductController.php` — создать: `show(string
      $slug)` (404 для неактивного / несуществующего)
- [x] `src/Views/product/show.php` — создать (по `SCR-03`; без вкладки
      «Reviews», без «Кредит/рассрочка», без «Наличие на складе»)
- [x] `src/Views/components/variant-selector.php` — создать: данные
      Вариантов и фото — в `data-*`/JSON в разметке, переключение на
      клиенте
- [x] `public/assets/js/app.js`, `public/assets/css/app.css` —
      изменить: смена Варианта → цена/срок/галерея; смена цвета —
      только галерея; блокировка «В корзину» без выбранного Варианта
- [x] `config/routes.php` — изменить: `/product/{slug}`
- [x] `src/Views/components/product-card.php` — проверено: ссылка на
      карточку уже вела на `/product/{slug}` с Таска 2, не трогал

Правка по ручному ревью пользователя после сдачи Таска (скриншот
карточки товара):
- [x] `src/Views/product/show.php` — изменить: главное фото обёрнуто в
      `.details-gallery-images > .single-img` (структура темы) вместо
      Bootstrap `.img-fluid` — исходники Task 1 (`assets/images/
      product/*.jpg`) физически 270×303px (так поставляются в самой
      теме, не наша ошибка копирования); `.img-fluid` = `max-width:
      100%` не увеличивает маленькое фото, оно повисало крошечным в
      большой колонке. `width: 100%` из `.details-gallery-images
      .single-img img` (`style.css`) растягивает под колонку — тот же
      приём, что уже работал в мини-карточках каталога
      (`.single-product img`), просто не был применён на карточке
      товара

Вторая правка по тому же ревью (новый скриншот, 4 отдельных
замечания):
- [x] `public/assets/js/app.js` — изменить: `applyContentOffset()`
      добавляет запас `+24px` сверх точной высоты хедера — раньше
      отступ был ровно равен высоте хедера, контент начинался
      впритык без «воздуха» (хлебные крошки визуально лепились к
      шапке)
- [x] `src/Views/product/show.php` — изменить: убран `pt-0` у секций
      «Характеристики»/«Похожие товары» — `.section-padding-02` даёт
      только `padding-top`, без `padding-bottom`; с `pt-0` на
      следующей секции между блоками не оставалось вообще никакого
      отступа (характеристики лепились к низу фото)
- [x] `src/Views/components/variant-selector.php`,
      `public/assets/css/app.css` — изменить: количество и «В
      корзину» — свой компактный `.product-variant-selector__quantity`
      (90px) в `flex`-ряду с кнопкой вместо `.single-form` (тема:
      `width: 100%`, рассчитано на поля логина, растягивало
      количество на всю ширину, а кнопка без отступа лепилась снизу)
- [x] `src/Views/product/show.php`, `public/assets/js/app.js`,
      `public/assets/css/app.css` — изменить: под главным фото
      добавлена галерея миниатюр (`#product-thumbnails`,
      `.details-gallery-thumbs`) — по фото на каждый цвет текущего
      Варианта, клик меняет главное фото и цвет (заменяет прежние
      текстовые кнопки «Цвет:», у темы нет готовой не-swiper вёрстки
      под это); контейнер миниатюр рендерится всегда (даже если у
      Варианта по умолчанию один цвет — тогда просто `hidden`), чтобы
      JS было куда подставить миниатюры при переключении на Вариант с
      несколькими цветами

Третий раунд правок по тому же ревью (новый скриншот, 3 замечания +
вопрос пользователя):
- [x] `public/assets/css/app.css` — изменить:
      `.product-variant-selector__lead-time` — `margin-bottom`
      16px → 24px (текст срока изготовления лепился к строке
      количество+кнопка); `.product-variant-selector__submit` —
      `flex: 1 1 auto` → `flex: 0 0 auto` + свой `padding` (кнопка
      растягивалась на весь остаток строки рядом с узким полем
      количества, выглядела непропорционально широкой)
- Диагностика (без изменений кода): «хлебные крошки не кликабельны» —
  не баг. Проверено настоящим headless-браузером (Playwright,
  установлен временно в scratchpad, не в проект): клик по ссылке
  реально доходит и открывает `/catalog/myagkaya-mebel` — просто
  ответ от реальной БД (`mikhail700.beget.tech`) занимает 4-5 секунд
  (подключение ~1.5с + каждый запрос ~300-400мс, а на странице
  каталога таких запросов несколько подряд). Для пользователя это
  выглядит как «ничего не происходит по клику». Не в скоупе Таска 4 —
  не связано с выбором Варианта, кода не менял

Четвёртый раунд правок по тому же ревью (скриншот: подвал лепится к
«Похожим товарам»):
- [x] `src/Views/product/show.php` — изменить: «Характеристики» и
      «Похожие товары» объединены в одну секцию `.section-padding`
      (низ+верх) вместо двух отдельных `.section-padding-02`
      (только верх). Раздельные секции гарантированно лепились бы к
      футеру, если последней рендерится ТОЛЬКО «Характеристики» — этот
      кейс реально существует на сидах: «Шкаф-купе «Гардероб»»
      («Шкафы») — единственный активный товар своей категории,
      `getRelatedProducts()` для него возвращает `[]` (второй товар
      категории, «Шкаф «Классик»», неактивен и исключён)

Проверено Playwright: `divan-krovat-oslo` (есть похожие товары) —
видимый отступ до футера на скриншоте; `shkaf-kupe-garderob` (похожих
товаров нет, единственный оставшийся раздел — «Характеристики») —
зазор от низа таблицы характеристик до футера 116px
(`footer.boundingBox().y - specsTable.boundingBox().bottom`), заголовок
«Характеристики» на месте (на первом скриншоте казался пропавшим —
артефакт `position: fixed` баннера cookie при full-page снимке, не
реальный баг: без баннера и с явной проверкой `.count()`/`boundingBox()`
заголовок присутствует). `composer test` — 63/63, `storage/logs/
app.log` не пополнился.

Пятый раунд правок — пользователь прислал скриншот баннера страницы
товара из оригинального макета (`product-details-affiliate.html`) и
загрузил реальный файл `public/uploads/baners/page-banner-product.webp`
(его собственное действие, не моё): попросил вернуть декоративный
баннер, которого в нашей карточке не было вовсе (сознательно пропущен
в Таске 4 — у темы он на `style="background-image:..."`, инлайн-стили
запрещены `general.md`).
- [x] `src/Views/product/show.php` — изменить: добавлена
      `.page-banner-section.page-banner-section--product` (заголовок —
      имя товара, `<h2>`, не второй `<h1>` — семантический `<h1>`
      остаётся один, в `.product-details-description`) с хлебными
      крошками внутри — тот же компонент `breadcrumbs.php`, разметка
      не менялась, только контекст (тема стилизует `.breadcrumb li`
      что внутри `.page-banner-content`, что снаружи). Убран
      `page-content-offset` с первой секции — с баннером хедер снова
      прозрачно перекрывает его верх по дизайну темы (как на Главной
      со слайдером), а не отступает от него
- [x] `public/assets/css/app.css` — изменить: `.page-banner-section--
      product { background-image: url(...) }` — картинка через
      CSS-класс, не через атрибут `style`, требование `general.md`
      выполнено буквально: фон есть, инлайн-стиля нет

Проверено Playwright: `background-image` баннера резолвится в
реальный `/uploads/baners/page-banner-product.webp` (200), полный
скриншот страницы визуально идентичен присланному оригиналу по
структуре (баннер с именем товара и крошками, хедer поверх). Остальные
фиксы (отступы, кнопка, размер фото) не пострадали — та же карточка
`divan-krovat-oslo`. `composer test` — 63/63, `storage/logs/app.log` не
пополнился.

Шестой раунд правок — пользователь показал скриншот блока
«Information/Description/Reviews» из оригинального макета и попросил
привести наш блок «Характеристики» к такому же виду (вкладки), без
вкладки отзывов (не показываем раньше Фазы 6):
- [x] `src/Views/product/show.php` — изменить: «Характеристики» и
      «Описание» стали вкладками `.product-details-tabs` (разметка и
      стили — 1:1 из `product-details-affiliate.html`/`style.css`,
      переключение — нативный Bootstrap `data-bs-toggle="tab"`, свой
      JS не понадобился); в «Информацию» подставлена наша таблица
      `product-specs-table` вместо текста-заглушки темы. Убран
      дублирующий абзац описания над селектором Варианта (в оригинале
      там и в блоке — РАЗНЫЙ текст-заглушка, у нас одно поле
      `products.description` — показывать его дважды на одной
      странице было бы избыточно). Нав с переключателем рендерится,
      только если есть ОБА раздела; если только один — просто
      показывается без вкладки-переключателя (нечем переключать)

Проверено Playwright: `divan-krovat-oslo` (специфика + описание) —
скриншот вкладок 1:1 повторяет стиль оригинала (оранжевая активная
вкладка с подчёркиванием, серая неактивная), клик по «Описание»
переключает без явного JS (Bootstrap data-api); `kreslo-kachalka-uyut`
(только описание, `product_specs` пустые) — нав не рендерится, текст
описания сразу виден. `composer test` — 63/63, `storage/logs/
app.log` не пополнился.

Седьмой раунд правок — скриншот: заголовок «Похожие товары» лепится к
таблице характеристик:
- [x] `src/Views/product/show.php`, `public/assets/css/app.css` —
      изменить: класс `mt-5` заменён на свой `.product-section-gap`
      (`margin-top: 48px`) — у темы `.mt-5` даёт всего 26px (свой
      нестандартный масштаб отступов, не Bootstrap-овские 3rem),
      рядом с плотной таблицей выглядело слипшимся

Проверено Playwright: `getComputedStyle` подтверждает зазор 48px
(было 26px), скриншот показывает отчётливый отступ. `composer test` —
63/63, `storage/logs/app.log` не пополнился.

Восьмой раунд правок — выравнивание текста во вкладке «Описание»:
- [x] `public/assets/css/app.css` — изменить: `.description-content`
      — `text-align: center` (тема) → `left`. Таблица характеристик и
      так по левому краю (ячейки `<th>`/`<td>`), правило точечно для
      вкладки «Описание»

Проверено Playwright: `getComputedStyle(.description-content).textAlign`
— `left`; скриншот подтверждает визуально. `composer test` — 63/63,
`storage/logs/app.log` не пополнился.

## Out of scope — не трогаем

- Поиск и подсказки (`/search`) — Таск 5
- Обработчик `POST /cart/add` и сама корзина — Фаза 2; форма на
  карточке рендерится, но сабмит даст 404 до Фазы 2
- CRUD товаров/вариантов для Менеджера/Администратора — Фаза 4
- Реальная загрузка фото в `public/uploads/products/` — Фаза 4; фото
  Варианта по-прежнему из сидов (пути темы)
- Скидки, отзывы (вкладка «Reviews» на карточке), избранное — Фазы 6/7
- Изменение сайдбара/фильтров каталога — Таск 3 закрыт
- Swiper-галерея с миниатюрами из темы — упрощено до одного `<img>`
  (см. DoD ниже, осознанное решение, не пропуск)

## Definition of Done

- [x] Ткань → экокожа: цена и срок изготовления меняются на значения
      Варианта; смена цвета в пределах Варианта — меняется только
      фото; цвет без реального фото → образец ткани (`is_swatch`), не
      битое изображение — структура и данные проверены `curl` против
      реальной БД (JSON по Варианту, `is_swatch=true` со своим валидным
      путём для «Коричневый» у `divan-verona`); сам клик — нет браузера
      в сессии, см. ниже
- [x] Вариант-образец: нет текста про срок изготовления, есть текст о
      готовности к выдаче; обычный Вариант — срок + текст о предзаказе
      рядом — проверено `curl`: `divan-verona` первый Вариант (Велюр,
      не образец) → «Срок изготовления: …»; второй (Экокожа) в JSON —
      `is_showroom_sample: true`
- [x] Ссылка «Buy Now» отсутствует; клик «В корзину» без выбранного
      Варианта (когда их > 1) блокируется с подсказкой; у товара с
      одним Вариантом он выбран по умолчанию — `grep -ic "buy now"` → 0;
      `data-preselected=""` и пустой `variant_id` при нескольких
      Вариантах, `data-preselected="8"` и заполненный `variant_id` при
      одном (`kreslo-komfort`); сама блокировка клика — JS, не
      воспроизведена в браузере
- [x] Форма «В корзину» — `POST /cart/add` с `csrfField()`,
      `variant_id`, `color`, `quantity` (обработчик — Фаза 2) —
      проверено `curl`, поле `_csrf` присутствует
- [x] Блок доставки — текст «стоимость уточняется при подтверждении
      заказа», без суммы — проверено `curl`
- [x] Таблица характеристик выводится из `product_specs` в порядке
      `sort_order`; товар без характеристик — блок скрыт, не пустая
      таблица — `divan-verona` (есть specs) рендерит таблицу,
      `kreslo-kachalka-uyut` (без specs) — 0 совпадений
- [x] «Похожие товары» — из primary-категории, без самого товара, без
      неактивных — проверено `curl`: на `divan-verona` показаны
      `divan-milan`/`divan-florenciya`/`divan-krovat-oslo`, самого
      `divan-verona` и неактивного `shkaf-klassik` нет
- [x] Неактивный товар → 404; весь вывод через `e()`, JSON Вариантов —
      `json_encode(..., JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS |
      JSON_HEX_QUOT)` — `shkaf-klassik` → 404; в `data-variants` нет
      сырых `<`/`>` (проверено `grep` на `<script`/`</script>`)
- [x] Проверить `.docs/dod-global.md` — `storage/logs/app.log` не
      прирастал новыми записями за время проверки (2 старые строки —
      побочный эффект скопившихся с прошлых тасков процессов
      `php -S` на одном порту, конфликтовавших за соединение с БД;
      устранено `kill`, не связано с кодом этого таска)

Не проверено (нет браузера в сессии): сам клик по кнопкам материала/
цвета и визуальная смена фото/цены/текста, блокировка сабмита формы
без выбора — код проверен вычиткой и синтаксически (`node --check`),
серверные данные, от которых JS отталкивается, проверены `curl`.

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
