# Current Task

## Фаза
Phase 6 — Скидки, отзывы и главная страница
(`.docs/phases/phase-6.md`), Таск 3 из 6.

**Статус:** ✅ Завершён 20.09.2026. Проверено на реальной БД живым HTTP
(`php -S` + `curl`) — без создания тестовых данных, на уже
существующей реальной скидке 20% на «Шкаф распашной «Классик»»
(`shkaf-klassik`). `composer test` — 274/274 (было 271/271, +3).
Подробности — `.docs/dev-log.md` 20.09.2026.

## Задача
`FR-CAT-010` — пятый фильтр сайдбара каталога: отбирает Товары, у
которых хотя бы один активный Вариант со скидкой (`FR-DISC-001`
правило 4); применяется без перезагрузки, как остальные (`FR-CAT-001`),
в URL — `on_sale=1`.

## Scope — что трогаем

- [x] `src/Core/CatalogFilters.php` — изменить: `normalizeCatalogOnSale()`
      (только `'1'` → `true`, всё остальное → `false` — тот же контракт,
      что `normalizeCatalogInStock()`), ключ `on_sale` в
      `normalizeCatalogFilters()` и `buildCatalogQueryString()`
      (добавляется в query-строку, только если `true`, — как `in_stock`)
- [x] `tests/Unit/CatalogFiltersTest.php` — изменить: зеркало
      существующих тестов `in_stock` — `on_sale=1` → `true`,
      `on_sale=0`/отсутствие/мусор → `false`; `on_sale` попадает в
      `buildCatalogQueryString()` только когда активен
- [x] `src/Models/Product.php` — изменить: `buildCatalogFilterConditions()`
      — новый блок `if (!empty($filters['on_sale']))`, алиас `pv5`:
      `EXISTS (SELECT 1 FROM product_variants pv5 WHERE pv5.product_id
      = p.id AND pv5.is_active = 1 AND pv5.discount_percent > 0)` — тот
      же паттерн, что `in_stock`
- [x] `src/Views/components/catalog-sidebar.php` — изменить: чекбокс
      «Со скидкой» рядом с «В наличии» (место уже отмечено комментарием
      в файле), `name="on_sale"`, `checked` по `$filters['on_sale']`

## Out of scope — не трогаем

- `src/Controllers/CatalogController.php` — не требует правок: вызывает
  `normalizeCatalogFilters($_GET)` напрямую, новый ключ подхватится сам
- `src/Controllers/SearchController.php` — у страницы поиска нет
  сайдбара с фильтрами вообще (только `sort`), фильтру «Со скидкой» там
  физически негде быть — не трогаем, не «при условии»
- `getFilterOptions()` — `on_sale` не нужны границы/опции (простой
  булев чекбокс, как `in_stock`, без своего виджета данных)
- `public/assets/js/app.js` — существующий селектор
  `#catalog-filter-form input:not(#catalog-price-slider)` уже слушает
  все поля формы, новый чекбокс подхватится без правок
- Новый индекс в `database.md` — `EXISTS`-подзапрос уже ограничен по
  индексированному `product_id` (тот же паттерн, что `in_stock`/цвет/
  `is_active`, которые тоже не имеют выделенного индекса под условие
  внутри `EXISTS`)
- Отзывы (Таск 4), модерация (Таск 5), блоки Главной (Таск 6)
- Изменение схемы БД — `discount_percent` заведена с Фазы 0
- Любой рефакторинг за пределами перечисленных файлов

## Definition of Done

- [x] Скидка у Варианта Товара → Товар в выдаче `on_sale=1` — проверено
      живым HTTP на реальной уже существующей скидке (`shkaf-klassik`,
      20%), без создания тестовых данных. Обратное направление (скидка
      снята → Товар пропадает) отдельно живым HTTP не гонялось — не
      стал переключать реальную скидку туда-обратно; логически
      гарантировано тем же условием `discount_percent > 0`, чьё
      поведение на `NULL`/`0` уже исчерпывающе проверено в Таске 1
      (`PriceTest`, живая БД) — тот же предикат, что здесь в `EXISTS`
- [x] `on_sale=1` переживает F5 и «назад»/«вперёд» браузера (общий
      механизм `app.js`, не переписывался); `on_sale=yes` (мусор) → 200,
      фильтр не применён, не 500 — проверено
- [x] Сочетание с категорией — `AND` (фильтр по категории, не
      содержащей Товар со скидкой, → пустая выдача) — проверено; с
      «В наличии»/ценой/цветом отдельно не гонялось (тот же
      SQL-паттерн `EXISTS`, что уже проверен для этих фильтров в
      прошлых фазах); пагинация/сортировка сохраняют `on_sale=1` —
      код не создаёт для него исключения в `buildCatalogQueryString()`
- [x] Пустая выдача по комбинации фильтров → существующее пустое
      состояние с «Сбросить фильтры» — проверено (категория без этого
      Товара); чекбокс в сайдбаре показывает `checked` при `on_sale=1`
- [x] `composer test` зелёный — 274/274 (было 271/271, +3 новых кейса
      `CatalogFiltersTest`)
- [x] Проверить `.docs/dod-global.md` — пункт про индекс закрыт
      обоснованием (Out of scope), не пропущен молча

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
- На каждый шаг — чем проверяется (пункт DoD / unit-тест / ручная
  проверка на реальной БД), не только что сделать
