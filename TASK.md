# Current Task

## Фаза
Phase 5 — Выставочный образец, резерв и возврат
(`.docs/phases/phase-5.md`), Таск 5 из 6.

**Статус:** ✅ Завершён 20.09.2026. Проверено на реальной БД и живым
HTTP (`php -S` + `curl`); `composer test` 248/248. Подробности —
`.docs/dev-log.md` 20.09.2026.

## Задача
`BR-003`/`BR-004` доведены до покупательских экранов: образец с активным
Резервом больше не «в наличии» — карточка показывает срок изготовления
вместо статуса образца, фильтр «В наличии» его не отбирает, попытка
добавить в корзину получает понятное сообщение вместо тихого добавления
недоступной позиции; сквозная проверка `FR-STOCK-005` — числового
остатка нет ни на одном экране.

## Scope — что трогаем

- [x] `src/Models/Product.php` — изменить: `buildCatalogFilterConditions()`
      — условие `in_stock` дополняется `AND NOT EXISTS (SELECT 1 FROM
      reserves r4 WHERE r4.product_variant_id = pv4.id AND r4.status =
      'active')`; `getProductVariants()` — добавить `has_active_reserve`
      (`EXISTS` по `reserves.status='active'`) в `SELECT`
- [x] `src/Controllers/ProductController.php` — изменить: в
      `$variantsData` — `'is_showroom_sample' => (bool)
      $variant['is_showroom_sample'] && !(bool)
      $variant['has_active_reserve']` — единая точка правки;
      `variant-selector.php` и `app.js` читают уже комбинированное
      значение под тем же ключом, без собственных изменений
- [x] `src/Core/Cart.php` — изменить: новая чистая функция
      `isReservedSampleUnavailable(bool $isShowroomSample, bool
      $hasActiveReserve): bool` — решает, можно ли добавить Вариант в
      корзину
- [x] `src/Models/Cart.php` — изменить: `addCartItem()` — запрос
      Варианта дополняется `has_active_reserve`; при занятом образце
      строка не создаётся и не увеличивается; сигнатура `bool` →
      `string` (константы результата `ADD_TO_CART_OK`/`_NOT_FOUND`/
      `_RESERVED`, по аналогии с `PREPAID_RESULT_*`)
- [x] `src/Controllers/CartController.php` — изменить: `add()` —
      `match` по результату `addCartItem()`, отдельное flash-сообщение
      для занятого образца
- [x] `tests/Unit/CartTest.php` — изменить: тесты
      `isReservedSampleUnavailable()` — обычный Вариант/свободный
      образец/занятый образец
- [x] `.docs/dev-log.md` — изменить: запись по таску
- [x] `.docs/phases/phase-5.md` — изменить: статус Таска 5,
      актуализация списка файлов с пометкой отклонений (пересмотр
      Scope согласован пользователем перед записью этого файла)

## Out of scope — не трогаем

- `src/Views/components/variant-selector.php`, `public/assets/js/app.js`
  — поведение уже корректно после правки `ProductController.php`:
  компонент и JS используют один и тот же комбинированный ключ
  `is_showroom_sample`
- `src/Views/components/product-card.php` — не показывал и не должен
  показывать статус образца; путь быстрого добавления в корзину уже
  покрыт исправленным `CartController`
- `src/Controllers/CheckoutController.php` — уже блокирует оформление
  при `hasUnavailable` понятным flash-сообщением (построено в Фазе
  2/3), не трогается
- `src/Views/components/cart-row.php`, `calculateCartTotals()` в
  `Core/Cart.php` — уже показывают «Недоступен для заказа — товар
  зарезервирован» и исключают позицию из суммы
- `updateCartItemQuantity()` — не меняется: `clampCartQuantity()` уже
  ограничивает любой образец количеством 1 независимо от резерва,
  увеличение сверх этого невозможно и без правок
- `mergeGuestCart()` — не добавляет новых резервированных позиций,
  только переносит уже существующие строки корзины, отображение их
  доступности не меняется
- Схема БД, `database/install.php`, `database.md` — без изменений
- Другие Model/Controller/View, не перечисленные в Scope; попутный
  рефакторинг

## Definition of Done

- [x] Каталог: фильтр «В наличии» не показывает Товар, чей единственный
      образец зарезервирован; после снятия резерва Товар возвращается
      в выдачу (проверка на реальной БД)
- [x] Карточка товара: у Варианта с активным Резервом — срок
      изготовления вместо «Выставочный образец — готов к выдаче», и
      при первой загрузке страницы, и при переключении Варианта кликом
      (через JS, без перезагрузки)
- [x] Прямой POST `/cart/add` на зарезервированный образец (минуя UI)
      → строка не создаётся/не увеличивается, понятное flash, не
      пустой редирект и не 500
- [x] Регрессия: оформление Заказа с позицией, ставшей резервированной
      между добавлением в корзину и оформлением, по-прежнему
      блокируется существующей проверкой `hasUnavailable`
- [x] Сквозной аудит (`FR-STOCK-005`): главная, каталог, карточка,
      корзина, чекаут — нигде нет числового остатка «N шт.»
      (регрессионная проверка, не новая разработка)
- [x] Новый `NOT EXISTS` в фильтре «В наличии» не требует нового
      индекса — использует существующие `reserves.status`/
      `product_variant_id` (`database.md`); пагинация каталога не
      ломается
- [x] `composer test` зелёный, включая тесты
      `isReservedSampleUnavailable()`
- [x] Проверить `.docs/dod-global.md`

## Решения таска

**Пересмотр Scope по факту кода — согласован пользователем перед
записью этого файла (AskUserQuestion).** Часть кода из списка файлов
`phase-5.md` для этого таска уже была построена заранее (Фазы 2–3):
`getCartItems()` уже вычисляет `is_reserved`, `calculateCartTotals()`
уже исключает резервированные позиции из суммы и помечает `available`,
`cart-row.php` уже показывает «Недоступен для заказа — товар
зарезервирован», `CheckoutController::store()` уже блокирует
оформление при `hasUnavailable`. Поэтому:
- `variant-selector.php`, `product-card.php`, `CheckoutController.php`
  — исключены из Scope: менять там нечего, поведение уже корректно
  либо чинится централизованно в другом месте.
- `ProductController.php`, `Core/Cart.php` — добавлены в Scope: там и
  находится единственная реальная точка правки для карточки товара
  (комбинирование `is_showroom_sample`/`has_active_reserve` один раз
  для SSR и для JSON, который читает `app.js`) и для чистой логики
  доступности образца в корзине.

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
- На каждый шаг — чем проверяется (пункт DoD / unit-тест / ручная
  проверка в браузере), не только что сделать
