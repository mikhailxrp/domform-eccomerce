# Current Task

## Фаза
Phase 8 — Админ-панель (контент и доступ) и статические страницы
(`.docs/phases/phase-8.md`), Таск 7 из 8.

**Статус:** ✅ Завершён 22.09.2026 — проверено на реальной БД живым
HTTP (`php -S` + `curl`), тремя временными пользователями ролей
(удалены после проверки): `admin`/`manager` → 200, `customer` →
редирект, гость → `/login`; новый баннер с фото появляется в слайдере
Главной в позиции `sort_order`, «Выключить»/«Включить» — слайд
пропадает/возвращается, строка остаётся; создание без фото —
отклонено; редактирование без нового файла сохраняет старое
изображение; замена файла удаляет прежний загруженный; `http://`/
`javascript:` ссылки отклонены, `/catalog` и `https://...` — приняты;
несуществующий id → 404 (edit) / flash (toggle); POST без CSRF → 419.

**Найден и исправлен реальный баг до коммита:** в первой версии
`store()`/`update()` файл физически сохранялся на диск ДО проверки
остальных полей формы — при валидном фото и, например, невалидной
ссылке форма отклонялась, а файл оставался осиротевшим навсегда.
Исправлено разделением на `validateUpload()` (только проверка MIME/
размера, без записи) и фактическое сохранение — только после того, как
`validateBannerInput()` подтвердит, что вся форма валидна. Перепроверено
живым HTTP: файлов на диске не прибавляется при отклонённой форме.

Все тестовые баннеры/файлы/пользователь удалены после проверки,
`storage/logs/app.log` — без новых записей. `composer test` —
395/395 (+12 `BannerFormTest`).

## Задача
`FR-ADM-003` п. 2, `FR-HOME-001` — `/admin/banners`: список (миниатюра,
текст, ссылка, порядок, активен); создание и редактирование с
загрузкой изображения; включить/выключить показ. Физического удаления
нет — тот же паттерн `is_active`, что у Товаров.

## Что проверено в коде перед планом
- `AdminCategoryController` — точный паттерн «одна форма на создание и
  редактирование» (`renderForm(?$entity, $old, $errors)`) —
  переиспользован для баннеров.
- `AdminProductController::toggle()` — паттерн `is_active` вкл/выкл.
- `storeContentImage()`/`deleteStoredFile()` (`ADR-046`) уже готовы —
  новых функций загрузки не понадобилось, `config/config.php` и
  `FileUpload.php` в Scope не вошли.
- `home.php` уже нормализует ведущий слэш `image_path` — Model
  баннера ничего дополнительно не обрабатывает.

## Scope — что трогали
- [x] `src/Core/BannerForm.php` — создан: `validateBannerInput()`
- [x] `tests/Unit/BannerFormTest.php` — создан (12 тестов)
- [x] `tests/bootstrap.php` — подключён `Core/BannerForm.php`
- [x] `src/Models/Banner.php` — добавлены `getAllBanners()`,
      `findBannerById()`, `createBanner()`, `updateBanner()`,
      `toggleBanner()`
- [x] `src/Controllers/AdminBannerController.php` — создан:
      `index()`, `create()`, `store()`, `edit()`, `update()`,
      `toggle()`, `validateUpload()` (проверка файла отделена от его
      физического сохранения — см. «найденный баг» выше)
- [x] `src/Views/admin/banners/index.php` — создан
- [x] `src/Views/admin/banners/form.php` — создан
- [x] `src/Views/layout/admin-header.php` — пункт «Баннеры»
- [x] `config/routes.php` — маршруты `/admin/banners*`
- [x] `.docs/dev-log.md` — запись по итогам таска (включая найденный
      и исправленный баг с осиротевшими файлами)

## Out of scope — не трогали
- `config/config.php`, `src/Services/FileUpload.php` — уже готовы
- Физическое удаление баннера — не предусмотрено
- Сотрудники/`admin`-only разделы — Таск 8
- Пагинация списка баннеров — не требуется

## Definition of Done
- [x] Новый баннер с загруженным фото появляется в слайдере Главной в
      позиции `sort_order`; «Выключить» → слайд исчез, строка осталась;
      «Включить» → вернулся. Сиды темы и загруженные баннеры
      отображаются одновременно
- [x] Создание без изображения — ошибка с подсветкой, файла на диске
      нет; редактирование без нового файла сохраняет старое
      изображение; замена файла удаляет прежний загруженный
- [x] Ссылка `javascript:...` / `http://` отклонена валидатором;
      несуществующий id на `/admin/banners/{id}/edit` → 404
- [x] `customer` → редирект; Гость → `/login`; POST без CSRF → 419
- [x] `composer test` зелёный (395/395, новые тесты
      `validateBannerInput()`)
- [x] Проверить `.docs/dod-global.md`

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
- Каждый шаг проверялся тем, что указано в DoD: слайдер/загрузка
  фото/404/CSRF — живым HTTP на реальной БД, чистая логика —
  `composer test`
