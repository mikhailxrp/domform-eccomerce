# Current Task

## Фаза
Phase 7 — Кабинет покупателя и уведомления
(`.docs/phases/phase-7.md`), Таск 1 из 9.

**Статус:** ✅ Завершён 21.09.2026. Проверено на реальной БД живым HTTP
(`php -S` + `curl`): временный тестовый Покупатель зарегистрирован,
проверен и удалён после проверки (`task1-test-customer@example.com`).
`composer test` — 296/296 (без изменений, регрессия). Реализация
полностью соответствует плану ниже, отклонений не потребовалось.

## Задача
Каркас личного кабинета: `/account` открывается только авторизованному
пользователю, показывает приветствие и сводку счётчиков, боковой
сайдбар с разделами кабинета; после входа Покупатель попадает в
`/account` (Менеджер/Админ — по-прежнему в `/admin`); в шапке —
пункт «Личный кабинет».

## Scope — что трогаем

- [ ] `config/routes.php` — изменить: добавить `GET /account`
- [ ] `src/Controllers/AccountController.php` — создать: `index()` —
      `requireAuth()` + сводка (заказы — через существующую Model
      Заказов; адреса и избранное — заглушка `0`, их Model появится в
      Тасках 4/6)
- [ ] `src/Views/components/account-sidebar.php` — создать: меню
      разделов по `my-account.html` (`account-menu-list`), активный
      пункт по `$activeSection`; вкладки «Download»/«Payment Method» из
      макета не переносятся (демо-артефакты темы, `screens.md`)
- [ ] `src/Views/account/index.php` — создать: заголовок, сайдбар,
      блок «Обзор»
- [ ] `src/Core/functions.php` — изменить: `homeUrlForRole()` → `/account`
      для роли `customer`/гостя, обновить комментарий
- [ ] `src/Views/layout/header.php` — изменить: пункт «Личный кабинет»
      в обеих копиях `dropdown-profile` (десктоп и мобайл, ~строки 110
      и 160)

## Out of scope — не трогаем

- `tests/Unit/AuthHelpersTest.php` — `homeUrlForRole()` тестом сейчас
  не покрыт нигде в проекте, новый тест не добавляем (проверено:
  `grep -rn homeUrlForRole tests/` — пусто)
- История заказов, адреса, избранное, личные данные, СМС — Таски 2–9
  этой же фазы
- `AdminController`, панель менеджера — не связаны с задачей
- Рефакторинг `header.php`/`functions.php` сверх нужной правки

## Definition of Done

- [x] Гость открывает `/account` → редирект на `/login` — проверено
      живым HTTP (`302` → `/login`)
- [x] Вход Покупателя → редирект на `/account`; вход Менеджера/Админа
      → редирект на `/admin` (регрессия `FR-AUTH-001`) — проверено на
      временном тестовом Покупателе и на `admin@domform.local`
- [x] Уже авторизованный Покупатель открывает `/login` или `/register`
      → редирект на `/account` — проверено для обоих маршрутов
- [x] На `/account` виден сайдбар с разделами и блок «Обзор» (счётчики,
      пока с нулями там, где Model ещё нет) — проверено HTML-дампом
      страницы, активный пункт «Обзор» подсвечен
- [x] Страница и сайдбар используют тот же контейнер/`.row`/`.col-*`,
      что уже проверенные на 320px `cart/index.php`/`checkout/index.php`
      (`page-banner-section` + `section-padding` + Bootstrap grid) —
      новых классов/вёрстки, требующих отдельной проверки, не добавлено
- [x] Пункт «Личный кабинет» в дропдауне шапки виден только
      авторизованному, ведёт на `/account`; «Выход» по-прежнему
      работает (POST + CSRF) — проверено живым HTTP, после выхода
      `/account` снова редиректит на `/login`
- [x] `composer test` зелёный (296/296, без изменений — регрессия)
- [x] Проверить `.docs/dod-global.md` — `storage/logs/app.log` без
      новых ошибок/warning от этого таска (сверено по времени с
      предыдущими записями лога)

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
