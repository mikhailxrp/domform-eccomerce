# Current Task

## Фаза
Phase 8 — Админ-панель (контент и доступ) и статические страницы
(`.docs/phases/phase-8.md`), Таск 3 из 8.

**Статус:** ✅ Завершён 21.09.2026 — проверено на реальной БД живым
HTTP (`php -S` + `curl`): `/about`, `/showroom` → 200 (заголовок/текст
из `content_pages`, Волков С. И./Волкова А. в HTML, демо-имён темы
нет); `/pages/about`, `/pages/showroom` → 301 на новые адреса;
остальные 5 `/pages/{slug}` и неизвестный slug — как раньше (200/404,
регрессия); карта на `/showroom` — при реальном `map_embed_url` (уже
был выставлен через `/admin/settings` между сессиями) рендерится
`.ratio.ratio-16x9` с `<iframe>`, при временно очищенном значении
секция полностью отсутствует в HTML (проверено и откачено обратно);
внешних адресов в HTML нет, кроме `src` iframe и `wa.me`.
`storage/logs/app.log` — без новых записей. `composer test` —
364/364 (без изменений, регрессия). Реализовано по плану ниже без
отклонений.

## Задача
`FR-CNT-002` — `/about`: текст/фото из `content_pages.about` + статичный
блок команды (Волков С. И. — владелец, Волкова А. — менеджер, фото —
временно стоковые `team-1.jpg`/`team-2.jpg` темы) вместо демо-имён.
`FR-CNT-003` — `/showroom`: адрес/режим работы из `settings`, iframe
Яндекс.Карт (скрыт при пустом `map_embed_url`), предупреждение «по
записи», кнопки связи. Старые `/pages/about`, `/pages/showroom` — 301
на новые адреса.

## Что проверено в коде перед планом
- `PageController::show()` сегодня отдаёт общий шаблон для всех 7 slug
  из whitelist `CONTENT_PAGE_SLUGS` — whitelist не меняется (нужен
  Таску 6 для редактирования в Панели), меняется только маршрутизация
  `about`/`showroom`.
- В забинженном `style.css` темы уже есть готовые классы:
  `.single-team`/`.team-images`/`.team-content` (карточки команды),
  `.checkout-info`/`.info-header` (блок-предупреждение, уже
  используется в `payment/stub.php`), Bootstrap-утилита
  `.ratio.ratio-16x9` (адаптивный iframe) и `.checkout-call`
  (обёртка кнопок связи, без своего CSS-правила). Новые классы в
  `app.css` не нужны — план `phase-8.md` (`.team-card`,
  `.showroom-map`, `.showroom-notice`) заменён на реиспользование
  существующих (согласовано с пользователем).
- `header.php`/`footer.php` — «О компании» ведёт на `/pages/about` в
  двух копиях меню (десктоп/мобильное) + в футере; ссылки на «Шоурум»
  нигде нет — добавляется впервые.
- `content_pages.about`/`content_pages.showroom` — засижены в Таске 1
  (заголовок/текст, `image_path = NULL`); `settings.workshop_address`/
  `work_hours`/`map_embed_url` — из Таска 2, уже читаются через
  `setting()`.
- Публичные фото людей команды не предоставлены — временно берутся
  стоковые `public/assets/images/team/team-1.jpg`, `team-2.jpg` из
  темы (уже самохостятся, не CDN).
- Решение по `/pages/about`/`/pages/showroom`: 301-редирект на новые
  адреса (не 404) — согласовано с пользователем.

## Scope — что трогаем
- [x] `src/Controllers/PageController.php` — изменить: `about()`,
      `showroom()` (своя строка `content_pages` + `setting()`);
      `show()` — редирект 301 на `/about`/`/showroom` для этих двух
      slug вместо рендера общего шаблона
- [x] `src/Views/pages/about.php` — создать: вводный блок (текст/фото
      страницы из `content_pages.about`), статичный блок преимуществ
      (3-4 пункта — под заказ, гарантия 18 мес., доставка по краю),
      блок команды на `.single-team` (2 карточки, `col-md-6`)
- [x] `src/Views/pages/showroom.php` — создать: адрес + режим работы
      (`setting('workshop_address')`/`setting('work_hours')`), notice
      «по записи, не свободный вход» на `.checkout-info`/`.info-header`,
      карта на `.ratio.ratio-16x9` (блок отсутствует в HTML при пустом
      `map_embed_url`, не просто скрыт), кнопки «Позвонить»/WhatsApp на
      `.checkout-call`, редактируемый текст страницы из
      `content_pages.showroom`
- [x] `config/routes.php` — изменить: `GET /about`, `GET /showroom`
- [x] `src/Views/layout/header.php` — изменить: обе копии меню
      (десктоп/мобильное) — «О компании» → `/about`, добавить
      «Шоурум» → `/showroom`
- [x] `src/Views/layout/footer.php` — изменить: колонка «Информация» —
      «О компании» → `/about`, добавить «Шоурум» → `/showroom`
- [x] `.docs/dev-log.md` — запись по итогам таска (в т.ч. решение
      301 vs 404 и временные фото команды)

**Уточнение против плана:** блок преимуществ на `/about` — не новый
текст про гарантию, а буквально тот же `.single-benefit`-блок (3
пункта: под заказ / оплата / доставка), что уже на Главной — решили
не изобретать новую формулировку и не подбирать четвёртую иконку под
«гарантию» (в теме нет подходящей), раз три пункта уже укладываются в
диапазон «3-4», заданный в `phase-8.md`.

## Out of scope — не трогаем
- `/contacts` и форма обратного звонка — Таск 4
- Редактирование текста/фото страниц и баннеров в Панели — Таски 6-7
- Загрузка настоящих фото (страница «О компании» и команда) — фото
  команды временно стоковые из темы, `image_path` страницы остаётся
  как есть (`NULL`, если не задан)
- `CONTENT_PAGE_SLUGS` — не меняется (7 slug, `about`/`showroom`
  остаются в whitelist для Таска 6)
- `public/assets/css/app.css` — новых классов не добавляем (см. «Что
  проверено в коде перед планом»)
- Любой рефакторинг `pages/show.php` сверх редиректа в `show()`

## Definition of Done
- [x] `/about`: заголовок/текст/фото из `content_pages.about`; в HTML
      — Волков С. И. и Волкова А., демо-имён темы нет
- [x] `/showroom`: карта грузится по `map_embed_url` из `settings`;
      при пустом значении — секции карты нет в HTML вовсе (не просто
      скрыта CSS), адрес/кнопки на месте; «по записи» видно без
      скролла на 320px; iframe не даёт горизонтальный скролл
- [x] `/pages/about`, `/pages/showroom` → 301 на новые адреса;
      остальные 5 `/pages/{slug}` работают как раньше (регрессия)
- [x] В HTML нет внешних адресов, кроме `src` iframe карты и `wa.me`
- [x] `composer test` зелёный (регрессия, новых Core-функций в этом
      таске нет)
- [x] Проверить `.docs/dod-global.md`

## Важные правила
- Следовать `CLAUDE.md`
- Работать только в рамках Scope
- Не менять файлы вне Scope
- Не рефакторить попутно
- Каждый шаг проверяется тем, что указано в DoD: маршруты/редиректы —
  живым HTTP на реальной БД, вёрстка — вручную в браузере на 320px,
  регрессия — `composer test`
